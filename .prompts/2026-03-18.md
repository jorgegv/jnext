# Tasks

## Task 1: design a general plan for emulator creation

- I want to create a software emuator in C/C++ for the ZXNext machine
- Analize the CLAUDE.md file and the FPGA repo mentioned there
- Design a high level plan to create a software emulator in C/C++ for the ZXNext computer, launching the following agents in parallel:
  - Agent 1: an expert project mmanager that coordinates the work of the other agents, with general expertise in technical matters
  - Agent 2: an expert VHDL engineer that fully understands deep patterns in VHDL code and how everything works
  - Agent 3: an expert C/C++ developer and systems engineer that is capable of generating highly optimized realtime code following best practices
  - Agent 4: a GUI designer expert in SDL library that can integrate it in any project in need of a clean but possibly sophisticated and portable user interface (Linux(MacOS/Windows)
  - Each agent should give its best with respect to the tasks that should be needed for the project so that the Manager (agent 1) can implement a sensible development plan
- Write the plan to EMULATOR-DESIGN-PLAN.md and let me iterate over it with you.
- Add a debugger panel
- The debugger must use the regular controls apropriate for the OS where the emulator runs. I.e. NOT ZesarUX style, but debugger should show a window like a regular application, with buttons text panels, etc. Suggest cross-platform UI libraries for achieving this (e.g. wxWidgets, Qt, GTK+, etc)
- Ensure the debugger has separate buttons for "Step Into" (runs step by step, following a call into the destination address), "Step Over" (same as Step Into, but if the instruction is a CALL, just run uninterrupted until the return instruction. E.g. by setting a temporary breakpoint on the instruction following the CALL)
