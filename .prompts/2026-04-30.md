
# Tasks

## Task 1: Continue parallax.nex investigation

Continuation of 2026-04-26 session. The visual delta between CSpect and
jnext on parallax.nex is **NOT explained by**: MMU mapping (byte-exact
verified), sprite anchor/relative, L2 image data, sprite multiplexing
across scanlines, CPU-speed timing skew, refresh-rate (50 vs 60 Hz —
user-verified at 50 Hz CSpect still shows rich content while jnext shows
sparse), per-scanline change-log vblank-flush gap (Task 2 fix — md5
identical pre/post on parallax), OR missing tbblue nexload init bundle
(Task 3 fix — also did not change parallax; if anything it made jnext
turbo by default which only deepened the timing-skew red herring).

Today's note (2026-04-29): with both Task 2 and Task 3 landed, parallax
is materially unchanged. The remaining suspect is the gap-doc's prime
candidate: **G117 — Copper executes per Z80 instruction, not per 28 MHz
cycle**. Quote: *"documented as a contributor to parallax.nex visual
divergence."* G117 deferred (cycle-accurate scheduler refactor, Effort
H multi-session).

The remaining unexplained gap is a real jnext-side bug. Plan:

### Phase A (start here): sprite priority / transparency / pixel-level compositor

The 96 sprites at y=112/y=128 DO render in jnext, but maybe their
priority vs L2 / ULA is wrong, hiding content. Investigate:
- NR 0x4A (sprite transparency index) — current value `0xE3` from demo
- NR 0x4C (sprite/tilemap palette select) — current `0x0F` from demo
- NR 0x6F (sprite-over-border) — default
- NR 0x68 (ULA blend mode) — default
- NR 0x15 SLU/LSU priority bits — `0x01` from demo
- Compositor priority handling vs VHDL `zxnext.vhd`

Audit each NR's handler in jnext for VHDL faithfulness when written.
Add per-pixel tracing in the compositor for parallax frame 250 to
identify where layer choices differ from CSpect's expected output (we
don't have CSpect-side per-pixel data, but we can verify jnext is
internally consistent).

### Phase B: decode bank 6 main loop

Disassembly covered IM1 vector install at 0x0053 and per-frame loop at
0x0090, but per-frame work calls were NOT yet traced:
- CALL 0x011E
- CALL 0x01EF
- CALL 0x0213
- CALL 0x0269
- CALL 0x02BF
- CALL 0x00F8
- CALL 0x0469

Decode each: what NR / port writes the demo issues each frame.
Cross-reference against trace at `/tmp/parallax-investigation/full-trace/`.
Find any NR or port jnext mishandles when the demo writes it.

**Note**: Z80N `NEXTREG` instruction is `ED 91 nn vv` (4 bytes), but
z88dk-dis treats it as 2-byte NOP — manual realignment required after
each `ED 91` for accurate disassembly. The init code at PC=0x8000
(NEX bank 2 lo, file offset 16896) was successfully decoded by hand.

### Phase C (consider after A + B): G117 cycle-accurate Copper scheduler

If A + B don't isolate the bug, the next step is the G117 architectural
refactor — Copper currently runs once per Z80 instruction; VHDL runs at
28 MHz. Tilemap-class effects (32 MOVEs per scanline) collapse to one
step per instruction in jnext. Effort H multi-session refactor; high
risk for regression rebaselines (G52). Discuss with user before starting.

### Reference

Full investigation history in
[doc/issues/PARALLAX-NEX-INVESTIGATION.md](../doc/issues/PARALLAX-NEX-INVESTIGATION.md).

User-supplied ground truth: `parallax-cspect-{1,2,3}.png` (CSpect at
5.0/5.2/5.5 s — rich game scene) vs `parallax-jnext-{1,2,3}.png`
(jnext at same time — sparse cave + bottom strip).

## Task 8: Work AGAIN on pending features (test cases with SKIP result)

Task 8 status as of 2026-04-29 EOD: **115/178 SKIPs closed** across
Tier 1 + Tier 2 + Tier 3 (unchanged from 2026-04-28). All remaining
SKIPs in those tiers are on the deferred-architectural list (see below).
Tier 4-6 untouched. Aggregate 3729/3544/0/185 (+6 from 2026-04-29 Task 2
PSCAN-VBLANK-* tests). Regression 34/0/0.

- Work on reducing new SKIPs identified in tasks 6 and 7 on 2026-04-27
- Work by subsystems. Don't go to the next one until one is finished, or I tell you so
- All work should be done by the usual expert: C++ plus VHDL
- Launch parallel waves of up to 5 simultaneous agents, one agent per feature
- Each feature in its own branch
- Allow agents to communicate if they have conflicts over the same code sections
- After each feature is developed, launch a new agent for reviewing the work
- This task shall not be considered closed until I tell you so

### Tier 1-3 deferred architectural blockers (open backlog)

These are ALL the Tier 1/2/3 SKIPs we did NOT close on 2026-04-28. Each
is blocked on a specific architectural concern that requires user
direction before tackling.

**Tier 1 — Display=Y, High** (52 remaining):
- mmu_test 32: G12/G16/G33-G38/G58/G146/G156 — multi-session/architectural.
- nmi_test 13: G46/G47/G48/G59/G60 — boot ladder + Multiface.
- layer2_test 2: G17 (parallax follow-on) + G28 (cycle-accurate refactor).
- compositor_test 2: G26 — FPGA-team Open Questions.
- divmmc_test 3: G46 NextZXOS boot + G136 SPI Flash backend.

**Tier 2 — Display=Y, Medium** (8 remaining):
- contention_test 5: **G141 + G53** — FUSE in-opcode contention macros
  re-enabled via ContentionModel + retirement bypass switch. Blocked on
  **G52 screenshot rebaselines** — CT-A agent on 2026-04-28 surfaced the
  expected timing-shift cascade and STOPped per brief. **User
  authorization needed for G52 rebaselines.**
- copper_test 3: **G117 + G65** — cycle-accurate Copper scheduler
  refactor (Copper-per-Z80-instr → Copper-per-28-MHz-cycle) + bus
  arbitration on tied 28 MHz edge. Effort H multi-session. Likely the
  parallax fix (gap doc says G117 contributes to parallax.nex visual
  divergence). **User decision needed: commit to refactor?**
- ula_integration_test 1: **G102 ULAnext** — palette widening 16→256 ×
  2 banks + G66 save-state schema bump. Effort H. (G103 closed
  2026-04-28 W5 with 64-entry palette; the renderer-integration shape
  is now a template for G102, but the bulk of effort — palette
  widening + save-state — remains.)

**Tier 3 — Display=Y, Low** (2 remaining):
- ula_test 2: **G104** — HI_RES 512px refactor (Effort M-H,
  compositor-level FB-width decision). **G105** — HI_RES 6-bit border
  palette-group encoding (becomes cheap once G102 lands; depends on
  G102's palette widening).

### Tier 4-6 (untouched as of 2026-04-28 EOD)

User explicitly said "if you need to go to Tier 4, then pause" on
2026-04-28 — paused until further direction.

  **Tier 4 — Display=blank, High** (10 skips, 1 suite):
    - [ ] rewind_test (10; top: G66 save-state schema + G67 rewind buffer assertion)

  **Tier 5 — Display=blank, Medium** (76 skips, 5 suites):
    - [ ] nextreg_test (42; top: G56 NextReg shadow-store + G151/G154/G149/G62/G64/G55)
    - [ ] sdcard_test (13; top: G158 SD hot-plug + G40/G41/G159/G160)
    - [ ] ctc_interrupts_test (9; top: G87 IM2 RETI/RETN + G89 LDIRX, G88/G90)
    - [ ] audio_nextreg_test (8; top: G110 mixer exc_i + G111/G112/G113/G73)
    - [ ] nmi_integration_test (4; top: G152 host F-key NMI dispatch)

  **Tier 6 — Display=blank, Low** (36 skips, 8 suites):
    - [ ] uart_test (10; top: G134/G135/G138/G139/G161/G39)
    - [ ] audio_test (9; top: G115 TurboSound::reset + G29/G30/G31)
    - [ ] audio_port_dispatch_test (5; top: G114 NR 0x84 DAC enables)
    - [ ] ctc_test (4; top: G119/G120/G121)
    - [ ] port_test (4; top: G45 expansion bus / cartridge framework)
    - [ ] dma_test (2; top: G122 DMA 14 MHz dma_d_p_s)
    - [ ] uart_integration_test (1; top: G135 NR 0xA0 Pi-UART routing)
    - [ ] nextreg_integration_test (1; top: G63 NR 0x03 machine-type latch)


## Task 12: NextZXOS boot — follow-on

Carried over (unchanged from 2026-04-26 / not advanced on 2026-04-28
or 2026-04-29).

Use ultrathink mode for nextzxos boot investigation.

Full history, hypotheses and commits in
[doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md](../doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md).

Today's note (2026-04-29): the Task 3 nexload-bundle landing means the
NEX-load init now matches tbblue. NextZXOS boot is a different code
path (BOOT, not NEX-load), so this didn't move it forward — but it
removes one class of red-herring "missing init writes" that could
otherwise have been confused with boot-ladder symptoms. The boot
ladder G46(a)/(b)/(c) still needs targeted DivMMC/RAM-test/firmware
work, not generic SKIP closures. G48 (Multiface) untouched. G153 (NR
0x02 reset_type FSM) still open as G46 contributor — schedule the
port-0xE7-write trace at the G46 stall window before promoting G153.

## Task Completion Status

- [ ] Task 1: parallax.nex investigation continuation. Phase A then B; Phase C (G117) on user signal. **2026-04-29 update: BOTH Task 2 (vblank-flush) and Task 3 (nexload bundle) fixes did NOT change parallax — md5 identical pre/post on both. Parallax remains an independent bug; likely G117.**
- [~] Task 8: SKIP-reduction. **2026-04-29 EOD: 115/178 SKIPs closed across Tier 1-3 (unchanged from 2026-04-28). All remaining Tier 1-3 SKIPs are deferred-architectural (G141/G52, G117/G65, G102/G66, G104, G105, G46, G48, G28, G26, G136, G33-G38, G58, G16, G12, G146, G156, G59, G60, G47). Tier 4-6 untouched per user pause-direction. Aggregate 3729/3544/0/185. Regression 34/0/0.**
- [~] Task 12: NextZXOS boot — PARKED.
