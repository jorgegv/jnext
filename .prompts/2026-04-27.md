
# Tasks

## Task 1: Continue parallax.nex investigation

Continuation of 2026-04-26 session. The visual delta between CSpect and
jnext on parallax.nex is **NOT explained by**: MMU mapping (byte-exact
verified), sprite anchor/relative, L2 image data, sprite multiplexing
across scanlines, CPU-speed timing skew, OR refresh-rate (50 vs 60 Hz —
user-verified at 50 Hz CSpect still shows rich content while jnext shows
sparse).

The remaining unexplained gap is a real jnext-side bug. Plan:

### Phase A (start here): sprite priority / transparency / pixel-level compositor

The 96 sprites at y=112/y=128 DO render in jnext, but maybe their
priority vs L2 / ULA is wrong, hiding content. Investigate:
- NR 0x4A (sprite transparency index) — current value `0xE3` from demo
- NR 0x4C (sprite/tilemap palette select) — current `0x0F` from demo
- NR 0x6F (sprite-over-border) — default
- NR 0x68 (ULA blend mode) — default
- NR 0x15 SLU/LSU priority bits — `0x01` from demo
- Compositor priority handling vs VHDL `zxnext.vhd`

Audit each NR's handler in jnext for VHDL faithfulness when written.
Add per-pixel tracing in the compositor for parallax frame 250 to
identify where layer choices differ from CSpect's expected output (we
don't have CSpect-side per-pixel data, but we can verify jnext is
internally consistent).

### Phase B: decode bank 6 main loop

Disassembly covered IM1 vector install at 0x0053 and per-frame loop at
0x0090, but per-frame work calls were NOT yet traced:
- CALL 0x011E
- CALL 0x01EF
- CALL 0x0213
- CALL 0x0269
- CALL 0x02BF
- CALL 0x00F8
- CALL 0x0469

Decode each: what NR / port writes the demo issues each frame.
Cross-reference against trace at `/tmp/parallax-investigation/full-trace/`.
Find any NR or port jnext mishandles when the demo writes it.

**Note**: Z80N `NEXTREG` instruction is `ED 91 nn vv` (4 bytes), but
z88dk-dis treats it as 2-byte NOP — manual realignment required after
each `ED 91` for accurate disassembly. The init code at PC=0x8000
(NEX bank 2 lo, file offset 16896) was successfully decoded by hand.

### Reference

Full investigation history in
[doc/issues/PARALLAX-NEX-INVESTIGATION.md](../doc/issues/PARALLAX-NEX-INVESTIGATION.md).

User-supplied ground truth: `parallax-cspect-{1,2,3}.png` (CSpect at
5.0/5.2/5.5 s — rich game scene) vs `parallax-jnext-{1,2,3}.png`
(jnext at same time — sparse cave + bottom strip).

## Task 2: NR 0x07 turbo handling defect (open backlog)

Surfaced 2026-04-26: writing `NR 0x07 = 0x03` (turbo 28 MHz) in
`Emulator::nex_init_machine` causes `tilemap-demo` (regression test)
to render BLACK SCREEN. Confirmed interactively. Likely a jnext-side
defect in turbo handling — possibly:
- Tight scanline-synchronized demo loops calibrated for 3.5 MHz miss
  their target at 28 MHz (could be VHDL-faithful behaviour but
  unfortunate)
- Effective T-state accounting at 28 MHz has a bug in jnext

Investigation: build a minimal NEX demo that just writes `NR 0x07 = 0x03`
and observe what visibly fails vs CSpect. Compare jnext's NR 0x07
handler against VHDL `zxnext.vhd` reset/runtime values.

## Task 3: 5 rebaseline candidates (paused)

Pre-emptively patching `nex_init_machine` with VHDL-faithful tbblue
init writes (NR 0x07 turbo, NR 0x42 palette format, NR 0x06/0x08
peripherals, NR 0x61/0x62 stop copper) shifted **5 of 6** screenshot
references in regression to outputs that visually align more with
CSpect — those are valid rebaseline candidates:
- `palette-demo`, `floating-bus`, `contention-test`, `dapr-sprite`,
  `dapr-tilemap_00`

PAUSED until Task 2 (turbo defect) is resolved. Patch reverted on
main; rebaselines are NOT current as of 2026-04-27.

## Task 4: prioritize video-related gaps for implementation

- Analize the KNOWN-FUNCTIONALITY-GAPS-AND-PLAN.md document and add a column with the subsystems that are affected by that gap
- Prioritize the known gaps that have any impact in the emulator display. We will give maximum priority to that.
- Sort the table according to that priority
- Dont modify any source code, just plan and report back (and commit the doc)

## Task 5: fix README and remove details about emulated subsystems

- Remove details about emulated subsystems in README. Instead put a reference to the KNOWN FUNCTIONALITY GAPS document and the UNIT TEST STATUS document. Commit the doc.

## Task 6: reassess VHDL subsystem functionality coverage

- Go over the VHDL sources subsystem by subsystem, verify all funcionality against the unit tests plan (for existing functionalities) and against the KNOW_ FUNCIONALITY GAPS documrnt (for already identified miswing functions), and identify any still missing functions.
- Add any missing functions still not identified to the known gaps document, and classify it according to the criteria indicated on task 4
- Launch an independent agent (in parallel) for each subsystem/function. You can launch up to a maximum of 6 parallel agents.
- After each subsystem finished, launch an independent review about the findings of that subsystem.
- After all subsystems have been reassessed and new gaps added to the gaps document, redo the priority list according to task 4 criteria
- Dont modify any source code, just plan and report back (and commit the doc)

## Task 7: add new test cases to current unit test plans for the known gaps analysis

- Using the Known Functionality Gaps document you enhanced in task 6, add new unit tests cases for ALL new 162 entries
- Consider the categorization of each entry and add it to the relevant existing unit-test plan, if it matches any of them, or create a new unit test plan if needed for any new category.
- Add the new items as SKIP or WONT, according to the choices already made for the Known Gaps Plan
- It's fine to re-open unit test plans that were already at 0 SKIP, we will have several of them open again, and will launch a second SKIP-closing plan after this task
- Do not touch existing source code AT ALL. Test suites can only be modified by adding new SKIP lines and tests. Develop code for running the test, but leave it as SKIP.
- You can launch as much as 10 parallel agents to work on this
- Do not launch independent agents for each new test. Group new tests by impacted subsystem for efficient work
- As usual, launch an independent reviewer for the code of each coder agent you launch.
- Try to be as autonomous as possible but be extremely careful with the code: this will be the golden standard by which the emulator features will be measured.

## Task 8: Work AGAIN on pending features (test cases with SKIP result)

- Work on reducing new SKIPs identified in tasks 6 and 7 on 2026-04-27
- Work by subsystems. Don't go to the next one until one is finished, or I tell you so
- All work should be done by the usual expert: C++ plus VHDL
- Launch parallel waves of up to 5 simultaneous agents, one agent per feature
- Each feature in its own branch
- Allow agents to communicate if they have conflicts over the same code sections
- After each feature is developed, launch a new agent for reviewwing the work
- This task shall not be considered closed until I tell you so

## Task 12: NextZXOS boot — follow-on (unchanged from 2026-04-26)

Carried over.

Use ultrathink mode for nextzxos boot investigation.

Full history, hypotheses and commits in
[doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md](../doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md).

## Task Completion Status

- [ ] Task 1: parallax.nex investigation continuation. Phase A then B.
- [ ] Task 2: NR 0x07 turbo defect (open backlog).
- [ ] Task 3: 5 rebaseline candidates (paused on Task 2).
- [x] Task 4: prioritize video-related gaps for implementation. Done 2026-04-27 — added Subsystems + Display columns to summary table in `doc/issues/KNOWN-FUNCTIONALITY-GAPS-AND-PLAN.md`, re-sorted display-affecting rows to top (30 of 86), revised Top-10 list to lead with display priority.
- [x] Task 5: README — replaced per-subsystem hardware bullet list with refs to KNOWN-FUNCTIONALITY-GAPS-AND-PLAN.md + test/SUBSYSTEM-TESTS-STATUS.md (commit `1e55a2b`).
- [x] Task 6: VHDL subsystem coverage reassessment. Done 2026-04-27 — 12 parallel coverage agents (2 waves of 6) audited all VHDL subsystems against jnext + existing gaps; 12 independent reviewers verified findings; consolidation + merge agent applied 76 NEW Gxx entries (G87-G162), 6 expansion-bullet blocks (G46/G48/G55/G56/G65/G71), 4 Appendix B WONT additions, 7 cross-bucket dedupes + 6 folds. Summary table re-sorted per Task 4 (53 display-affecting now lead, 109 non-display follow). Top-10 refreshed: G106 line-int off-by-one + G91 NR 0x44 priority + G117 Copper-per-Z80-instr + G141 FUSE in-opcode contention + G126 NR 0x05→MembraneStick added. Doc 2018 lines (was 1170). Commit `127b915`.
- [x] EOD cleanup: flipped 3 stale checkboxes in `doc/design/EMULATOR-DESIGN-PLAN.md:1115-1117` (Floating Bus 32/32, Contention 68/68, VideoTiming 22/22 — all already closed in dashboard). Contention runtime wiring residual remains tracked as G50/G141/G142 in the gaps doc. Commit `55434ea`.
- [x] Task 7: add SKIP test rows for G87-G162. Done 2026-04-27 — 10 parallel test-plan agents authored patch descriptions; 10 independent reviewers verified (6 blocking issues caught — skip helper signatures, G89 expansion to 4 opcodes, B10 §16 collision, B6 plan total, etc.); applier agent + plan-doc backfill agent merged on isolated worktree; merged to main as `e9a4cd1`. Net: +133 SKIP rows across 15 test sources + 15 plan docs + dashboard, 0 src/ touched, 0 new check() calls. Unit 3517/3384/0/133, regression 34/0/0 (after clean rebuild fixed stale-build-artefact false-failure earlier in session).
- [~] Task 12: NextZXOS boot — PARKED.
