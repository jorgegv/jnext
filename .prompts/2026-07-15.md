# Tasks — 2026-07-15

Baseline at the start of this file: **main @ b0d57d2d · unit 4642 (4642 pass / 0 fail /
0 skip — ZERO skips suite-wide, first time; 47 suites) · FUSE 1356/1356 · regression 84/0/0.**

Continues `.prompts/2026-07-14.md` (Task 27 Wave 1 + Tasks 55–60a/60b + Task 13a all closed
there — see that file's Task completion status). Model switched mid-day (Fable → Opus) after the
Wave-1 handover. Session state in memory: `project_session_handover_2026-07-15_eod.md`.

Done today (housekeeping, after the Wave-1 handover):
- **Task 13a RECLASSIFIED** — Next-specific snapshot schema is a Phase 11 future enhancement, not
  a gap; 7 SS-VER skip rows removed → unit suite reaches **0 skips**. Merged `2746c708`.
- **Dashboard reconciled** — `test/SUBSYSTEM-TESTS-STATUS.md` was undercounting by 250 rows (11
  whole suites had no row); added them, Total now 4642 = live suite; green rows now all say
  "All tests pass." (script always did this — the file had been hand-edited). Added a non-fatal
  **drift guard** to `make unit-test` (warns when a count changed or a live suite has no
  dashboard row). Merged `b0d57d2d`.
- **`PROFILING-OPTIMIZATION-PLAN.md` marked OBSOLETE** (banner → TASK27 docs; its item C2 is
  WRONG — do not implement).
- **v0.99.0 ChangeLog record OPENED** (unreleased, deliberately left open for more entries).

## Task 27: performance optimization — WAVE 2 (PROPOSED, NOT LAUNCHED)

Wave 1 COMPLETE, primary DoD MET: boot-nextzxos 57.4M→**114.8M T-states/s (+100%)**, **202.4 fps
≥ 200 @ 400%** (settled-box, `test/bench/baseline-db3f2136.txt`). 14 branches merged, all
independently reviewed. Full detail: `doc/design/TASK27-OPTIMIZATION-PLAN.md` "WAVE 1 RESULT" +
"WAVE 2 — PROPOSED" sections; profile in `TASK27-PROFILE-REPORT.md`.

**Wave 2 = 3 tracks, user picks the subset (NOT started, awaiting confirmation):**
- **Track A — copper/beast gap** (only these two are still short of their own 400% extrapolations;
  boot-nextzxos, the real DoD, is met). P2 re-profile scoped to copper-demo/beast FIRST (P1's
  C7/C8 kill was DoD-scoped only), then **C9** (Copper per-master-cycle div/mod — copper's
  dominant cost), then C7/C8 as re-ranked. **Open question for user: does this gap matter, or is
  400%-on-the-OS/games-path (met) enough?**
- **Track B — broad margin**: **C10+glue** (Z80Registers copies, getenv guard, dead on_contention
  store, FUSE wrapper sync 5.95%), **B1/LTO** (full triplet gate).
- **Track C — correctness tail (Task 60 remainder)**: see Task 60 below.

Protocol for any wave: §0.4b (parallel workers, interleaved A/B under exclusive bench lock,
independent review, sequential merges with rebase+re-measure). Merge-on-green authorized;
pushing is NOT (user pushes).

## Task 60: bugs found during Task 27a

- **60a DONE** (merged `1fdcdaaf`) — debugger single-step now delivers interrupts (shared
  `step_one_instruction`).
- **60b DONE** (merged `99714612`) — StateWriter/Reader bounds + per-subsystem sentinels + G67;
  failed rewind pauses + propagates false.
- **60c PENDING** — `src/input/` absent from snapshots (keyboard/joystick/mouse/MD6 state lost on
  rewind; MD6 has a live FSM ticked every instruction). Add `save_state`/`load_state` to the
  input subsystems + wire into `Emulator::save_state` (mind 60b's sentinel ordering — adding a
  subsystem = one save call + `put_sentinel()`, matching load + `check_sentinel("name")`).
- **60d PENDING** — IM2 pulse counter runs 8× fast at 3.5 MHz. Commit `314a752d` claims the caller
  passes `tstates` not `master_cycles`; git proves it never did (`emulator.cpp:6619` always passed
  `master_cycles`). Investigate vs `zxnext.vhd:2037-2044` — fix the code OR the (VHDL-cited but
  contradicted) comment; one of them is wrong.
- **60e PENDING** (from 60b re-review) — the GUI can one-click-resume (F5) a corruption-paused
  machine with no warning. Wire `Emulator::last_state_error()` into a status-bar/dialog warning in
  `DebuggerManager::on_step_back`/`on_rewind_to_frame`, and make `on_run()` aware a
  corruption-pause should not silently resume.

## Task 61 (backlog, low priority): SUBSYSTEM-TESTS-STATUS.md is a stale-count magnet

Reconciled today, but the deeper issue: it only refreshes on the explicit `make unit-test-dashboard`
target, so it rots between runs. Today's drift guard (warns in `make unit-test`) is the mitigation.
If it keeps drifting, consider auto-refresh (rejected today: dirties the tracked file in every
parallel worktree, causing merge noise) or a pre-commit hook. Also: the doc still shows only a
subset of the deeper per-subsystem history; a from-scratch rebuild keyed off `unit-tests.conf`
would be more robust than the current preserve-rows-verbatim script.

## Task completion status

- [ ] Task 27 Wave 2 — PROPOSED (Tracks A/B/C), NOT launched, awaiting user pick
- [ ] Task 60c — input serialisation missing from snapshots — PENDING
- [ ] Task 60d — IM2 pulse-counter units 8× fast at 3.5 MHz — PENDING
- [ ] Task 60e — GUI resumes corruption-paused machine with no warning — PENDING
- [ ] Task 61 — dashboard stale-count robustness — BACKLOG
- [x] Task 13a — RECLASSIFIED to Phase 11 (merged 2746c708)
- [x] Dashboard reconcile + drift guard + PROFILING-doc obsolete (merged b0d57d2d)
