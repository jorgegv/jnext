# Tasks

## Task 1: NextZXOS boot — PRIMARY FOCUS

Trace and unblock the SKIPs that gate NextZXOS booting end-to-end. This was Task 12 (PARKED) and the firmware-faithful side of G46. Today's session (2026-05-03h) closed G46(a); the other two blockers remain.

### Background

G46 = "NextZXOS boot ladder" — single multi-blocker root entry:
- **(a)** ✓ CLOSED 2026-05-03h. Proper VHDL `divmmc.vhd:131` delayed-off automap_held clear via 1-M1-cycle delay register inside `DivMmc`, driven from `Im2Controller::retn_seen_this_cycle()` via new `DivMmc::on_m1_retn_delay(bool)` callback. The pre-G87 RETN-alias band-aid is fully retired.
- **(b)** RAM-test outer-loop infinite-loop hang — firmware cycles 112 RAM banks via NR 0x56, ~208 passes/bank in 15 s, never exits. Possibly NR 0x1E/0x1F (active video line) timing skew.
- **(c)** Missing logo + earlier loader log lines (real Next shows 4-entry log + diagonal colour bars; jnext shows enNextZX.rom only). Diagnosis: rendering gap, not peripheral.

### G-tasks in scope

- **G46** — NextZXOS boot ladder (parts b+c remaining)
- **G47** — NextZXOS post-boot regression / dot-command surface
- **G48** — Multiface peripheral skeleton (NMI consumer)
- **G59** — boot ladder bypass option
- **G60** — boot ladder
- **G153 plausibility re-check** — port-0xE7-write trace at G46(b) stall window (NR 0x02 reset_type FSM affects SPI-Flash-CS at first power-on; `--boot-rom` flow bypasses the FPGA-Flash path entirely so plausibility was rated "moderate, not likely" — re-verify post-G46(a))
- **mmu_test G16** — NEX-loader bank-5 collision (may relate to boot path)

### Test surface

`nmi_test` has 13 open SKIPs covering G46/G47/G48/G59/G60. Full audit at session start.

### References

- [doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md](../doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md) — full chronological investigation history (Task 12 legacy)
- [doc/issues/KNOWN-FUNCTIONALITY-GAPS-AND-PLAN.md](../doc/issues/KNOWN-FUNCTIONALITY-GAPS-AND-PLAN.md) — G46/G47/G48/G59/G60 entries
- TBBlue source tree: `/home/jorgegv/src/spectrum/tbblue/`
- NextZXOS test image: `roms/nextzxos-1gb-fat32fix.img` (fat32 cluster fix) per CLAUDE.md

### Approach (suggested, refine at session start)

1. Re-baseline: run `./build/jnext --machine next --boot-rom roms/nextboot.rom --divmmc-rom roms/enNxtmmc.rom --sd-card roms/nextzxos-1gb-fat32fix.img` and capture current stall behavior post-G46(a).
2. **G46(b) RAM-test loop**: trace NR 0x56 + NR 0x1E/0x1F writes during the 15 s loop. Compare with TBBlue firmware source.
3. **G46(c) missing logo**: capture early framebuffer state. Compare jnext logo render to real Next 4-entry log + diagonal bars. Likely a per-scanline NR/timing replay gap on top of one of the rendering paths.
4. Use ultrathink mode for investigation phases (per Task 12 legacy guidance).

### Working rules (carried over)

- All work via expert agents (C++ + VHDL fluent). Manager does not write code.
- Each feature on its own branch; up to 5 parallel agents authorized.
- Independent reviewer for every change (per `feedback_never_self_review.md`).
- For any rendering-adjacent src/ change: reviewer MUST run screenshot regression on representative demos and md5-compare to references (per `feedback_review_run_screenshot_regression.md`).
- `LANG=C` on every `make` / `cmake` invocation; `JNEXT_TEST_JOBS=4` for regression; `make clean && make gui-release` before regression runs.
- Worktrees go in `.claude/worktrees/<name>` (per `feedback_worktree_location.md`); never sibling dirs (per `feedback_no_files_outside_repo.md`).

## Task 8: SKIP-reduction (residual deferred-architectural)

- **mmu_test** (32 open) — file-format loaders (SNA/SZX/Z80/TZX/etc., G33-G38; overlaps Task 13b) + G12 Nirvana mux + G16 NEX-loader bank-5 collision (Task 1 candidate) + G58/G146/G156 multi-session.
- **nmi_test** (13 open) — boot ladder + Multiface (subsumed under Task 1).

Both are multi-week feature work. Pick off as opportunistic alongside Task 1.

## Task 12: NextZXOS boot — UNPARKED → merged into Task 1

History preserved in [NEXTZXOS-BOOT-INVESTIGATION.md](../doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md). All active work tracked under Task 1 going forward.

## Task 13: Snapshot / Save-State work

### 13a — Next-specific snapshot schema (G66 + G67) — PARKED

Low priority, needs thoughtful design before any code is written. Open architectural questions (tag assignment, migrator API, unknown-tag policy, magic placement, backwards compat at v1). Coverage: rewind_test 10 SKIPs (SS-VER-01..07 + RB-FRAME-01..03) all marked `(see G66)` / `(see G67)`. Resume trigger: when the user wants persistent / cross-version save-states.

### 13b — Standard snapshot formats (.SNA / .SZX / .Z80 save) — backlog (G35)

Fully specified by existing Spectrum / Next ecosystem; implementation should be straightforward and does NOT depend on 13a. `SnaSaver` already exists but unwired to GUI/CLI; `.szx` and `.z80` writers don't exist yet. Scope: File→Save Snapshot (Ctrl+Shift+S) + `SzxSaver` mirroring `szx_loader.cpp` + optional `.nex` saver. Effort M, user-visible payoff. Queued behind Task 1.

## Task Completion Status

- [ ] **Task 1** — NextZXOS boot (PRIMARY FOCUS): G46(a) closed 2026-05-03h; G46(b)+(c) + G47/G48/G59/G60 remain. Baseline aggregate **3791/3736/0/55**.
- [ ] **Task 8** — residual Tier 1 deferred-architectural: mmu_test (32) + nmi_test (13, subsumed under Task 1).
- [ ] **Task 13a** — Next-specific snapshot schema (G66+G67), PARKED.
- [ ] **Task 13b** — Standard snapshot formats (.SNA/.SZX/.Z80 save, G35), backlog.
