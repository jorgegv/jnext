# Tasks — 2026-07-14

Baseline at the start of this file: **main @ c35d3107 · unit 4575 (4554 pass / 0 fail /
21 skip, 47 suites) · FUSE 1356/1356 · regression 64/0/0 · harness-selftest 26/26.**

Carried over from `.prompts/2026-07-13.md` (everything else there is closed — see that file's
Task completion status). Merged overnight 2026-07-14: Task 54 + G12 mux (BIFROST/Nirvana solved,
pixel-identical to FUSE), Task 51, ULA clip NR 0x1A; Task 52 closed with its analysis archived
in doc/issues/g12-nirvana/G12-NIRVANA-STATUS-2026-07-13.md §9.

Pending user decisions (not tasks): push to origin (~35 commits ahead); remove stale
prior-session worktrees (task48-audio-mute, task48/49/50-review, task49-exit-frames,
task50-contention).

## Task 55: bifrost/nirvana/nirvanap regression screenshot tests

- USER AUTHORIZED 2026-07-14 ("You will do nirvana/bifrost tests, but in a new session").
- TAPs are committed at `test/00regression/tap/{bifrost,nirvana,nirvanap}.tap`.
- Capture recipe (proven in the Task 54 session):
  - jnext: `--headless --machine 48k --load <tap> --delayed-screenshot-frames N
    --delayed-automatic-exit-frames N+2`. BIFROST screen 2 ("Demonstrating static tiles") via
    `--delayed-keypress-frames 500 SPACE`, capture ≥ frame 900.
  - BIFROST screen 1 has FLASH cells — pick the reference frame deliberately; frames 16 apart
    flip the phase. Nirvana ANIMATES — headless is frame-deterministic so any fixed frame is a
    stable reference.
  - Verify each reference against real FUSE before committing it: Xvfb + `xdotool windowfocus
    --sync $WID` + plain `xdotool key space` (XTEST; `key --window` does NOT reach FUSE);
    crop `768x576+96+100`, `-sample 256x192`; compare COLOUR INDICES (FUSE normal=0xC0,
    jnext normal=0xDB RGB333 — map 192→219 then raw-diff must be 0 on static screens).
- Manifest discipline: conf entries + `img/<name>-reference.png` (the harness has an
  independent img/-witness check).
- **DONE 2026-07-14, merged @ 2af14d35** (independent APPROVE). 4 tests (bifrost-screen1 f300
  flash-phase-A, bifrost-screen2 f900+SPACE@500, nirvana/nirvanap f500), expect 46→50. Both
  bifrost refs exactly 0 px vs real FUSE (colour-index mapped); nirvana/nirvanap verified on
  static rows + colour-index sets (animated content untestable frame-exact); reviewer proved
  all 4 discriminative vs pre-Task-54 code.

## Task 56: runtime NR 0x05 50/60 Hz change never re-inits video timing

- Follow-up found during Task 51: NR 0x05 bit 2 (`nr_05_5060`) is stored (and Pentagon-gated
  on read) but `video_timing_` is only ever init'ed with `refresh_60hz=false` at
  `Emulator::init()` — a runtime 50/60 toggle (F3 hotkey or direct NR 0x05 write) changes
  nothing.
- Same seam as Task 51: `Emulator::repush_video_timing_from_machine_timing()` already
  preserves `video_timing_.refresh_60hz()`; this task makes the NR 0x05 write path drive it
  (check the VHDL for whether the 50/60 switch is frame-edge-latched like tim_sel or
  immediate) and re-push.
- Tests per the Task 51 shape (VT rows + an integration row), mutation-tested, own branch +
  independent review.
- **Progress 2026-07-14: code+tests complete on branch task56-nr05-5060 @ c7050fd4, independent
  review running.** VHDL: frame-edge-latched (zxnext.vhd:6697-6700, same vsync seam as tim_sel;
  pending FF :5834-5842, Pentagon forces 0; bit 2=1 → 60 Hz). Rows VT-T56-01..04 + T56-INT-01,
  mutation-tested. Unit 4580/0 fail; regression 64/64 (two earlier FAILs were CPU-load
  contention from parallel suites — confirmed by isolation re-run + NR trace showing bit 2=0 on
  every boot write).
- **Follow-up found (own task later): NR 0x05 bit-2 READBACK returns the pending value; VHDL
  zxnext.vhd:5897 returns the frame-latched effective one. Existing rows encode pending
  semantics — needs its own test-plan pass.**

## Task 57: SKIP-reduction residual from Task 8 (mmu_test 9 + nmi_test 1)

- Migrated from 2026-07-12's "Task 8: SKIP-reduction (mmu_test 22 + nmi_test 6)" — 8a/8b/G12
  closed most of it (22→9, 6→1) but the residual was never re-filed (user caught this
  2026-07-14).
- mmu_test 9 skips:
  - SD2-01/02 (×2) — Soundrive Mode-2 paging suppression (`port_fd_conflict_wr`,
    zxnext.vhd:2708,2718-2720) not modelled (G146). **Most tractable — start here.**
  - BOOT-TAPESAVE-01..03 (×3) — blocked on tape-save infrastructure (G33).
  - BOOT-DECI-01..04 (×4) — blocked on TZX 0x15 / WAV real-time DeciLoad (G36/G37, the
    design plan's open DeciLoad issue).
- nmi_test 1 skip: BOOT-DOT-01 — dot-command shell end-to-end (.ls), genuinely unreachable
  from the unit tier (G47); candidate for a functional regression test instead.
- (Out of this task's scope but same neighbourhood: rewind_test 10 skips, port_test 1.)
- **Progress 2026-07-14 (agent team, user directive "SD and BOOT SKIPs"):**
  - **SD2 (G146) MERGED @ c7ef2dad** — `decline_write()` fall-through in port dispatch; the
    real bug was gate-OFF (SD2 handler swallowed paging writes with NR 0x84 b2 clear). Rows
    SD2-01/02 re-homed to audio_port_dispatch_test (COVERED-AT comments in mmu_test); PR-DECL-01
    added in port_test after reviewer REJECT round (untested `write_declined_` nested-leak fix).
    IMPORTANT doc correction: colliding addresses have LOW byte 0xF1/0xF9 (e.g. 0x7FF1), NOT
    "0xF1FD"; gate is NR 0x84 **bit 2**, not bit 1 — G146/plan-doc text was wrong, now fixed.
  - **Tape-save (G33 Phase 1) in re-review** on branch task57-tapesave: TapSaver + SA-BYTES trap
    at 0x04C2 + `--tape-save`; BOOT-TAPESAVE-01..03 un-skipped; FUSE foreign-reader load-back OK.
    Reviewer REJECT round found the trap false-firing 10× on a NextZXOS boot (corrupted file +
    broken boot) — fixed with a ROM-identity gate (bytes at 0x04C2 must be `21 3F 05 E5`) +
    MMU-G33-TRAP-01..03 + functional row tape-save-boot-func. GUI menu = follow-up.
  - **Tape-save (G33 Phase 1) MERGED @ b0d40ae1** (APPROVE after the false-fire fix round).
  - **BOOT-DOT-01 (G47) MERGED** — `boot-nextzxos-dotls` regression row (menu → Command Line →
    `.ls`, listing verified vs the SD image via mdir), `--delayed-keypress-frames` extended
    (punctuation as SYM compounds, named keys, `sym+`/`caps+`, unknown names exit 1); nmi_test
    skip → COVERED-AT (58→57). Side catch: CT-INT-03 only read the first 4 KB of the conf.
  - **DeciLoad (G36/G37) MERGED @ 0b3c5e81** (APPROVE after 1 fix round). THREE root causes,
    none of the old doc's theories: (1) frame-relative tape clock — begin_new_frame() zeroes
    FUSE tstates, ZOT edge_clock is absolute → ALL real-time TZX/WAV frozen since 2026-04-13;
    fixed with Emulator::monotonic_tstates() (serialized in save/load state after review).
    (2) ZOT pause swallowed each block's terminating edge — 1 ms hold-then-low (documented as
    an empirical heuristic; libspectrum does NOT do this — reviewer checked the source).
    (3) WAV edges quantised to the 79.4 T sample grid — sub-sample interpolation. Xevious TZX
    and Dizzy WAV both load end-to-end, locked by xevious-deciload + dizzy-wav-deciload rows.
    Honest note: FUSE could NOT be made to load the DeciLoad TZX headless — the old "FUSE
    handles it" claim didn't reproduce; verification is determinism + ZX0-integrity argument.
  - **Task 57 COMPLETE: mmu_test and nmi_test skips → 0.** Unit suite skips now 11 total =
    rewind 10 (all Task 13a / G66+G67) + port 1 (V18-NMP-NIT-01c observability nit).

## Task 58: repush-seam follow-ups from Task 56 review (DONE 2026-07-14)

- Filed 2026-07-14 from the Task 56 independent review (APPROVE with follow-ups):
  1. **Copper `c_max_vc_` staleness (MAJOR, pre-existing since Task 51)**: `Copper::set_c_max_vc`
     is called once in `Emulator::init()` (emulator.cpp:~1963) with the initial 50 Hz
     `timing_.lines_per_frame`, never from `repush_video_timing_from_machine_timing()`. After a
     runtime NR 0x03 tim_sel or NR 0x05 50/60 change, Copper WAIT vertical wrap goes stale
     (VHDL zxula_timing.vhd:462 reloads cvc wrap combinationally on c_max_vc). Fix: add
     `copper_.set_c_max_vc(...)` inside the repush seam (covers both axes); + a test row.
  2. **NR 0x05 bit-2 readback semantics**: jnext returns the PENDING bit; VHDL zxnext.vhd:5897
     returns the frame-latched EFFECTIVE `eff_nr_05_5060`. Existing rows (nextreg_integration,
     input FNK-02) encode pending semantics — needs its own test-plan pass, not a drive-by.
- **DONE 2026-07-14, merged (independent APPROVE, zero findings).** Item 1: `set_c_max_vc`
  re-pushed in the repush seam (T58-CVC-01 mutation-tested). Item 2 grew justified scope: the
  VHDL read mux latches BOTH bit 2 (50/60) and bit 0 (scandouble) at the frame edge — read
  handler now serves both from effective state; the read-time Pentagon mask was removed as
  not VHDL-faithful (Pentagon forces only the pending FF, :5835-5836). 7 wrong-oracle rows
  corrected to VHDL-derived expectations (G56-CR-NR05-02/03/04, TC-NR05-PRESERVE/PENTAGON,
  V13-NMP-01, FNK-02/03) + new T58-NR05-EFF-01. Unit 4588/0/11 · regression 80/80.

## Task 13a: Next-specific snapshot schema (G66+G67) — PARKED

- Migrated from 2026-07-13 (and earlier). `.szx` structurally cannot carry Next state; a
  Next-specific schema is the real answer. Still parked — do not start without the user.

## Task 27: performance optimization

- Migrated from 2026-07-13 (and earlier). NOT STARTED (user: "it's huge").
- Now also owns the G12 attribute-mux always-on detector cost (~5-10%, measured; user accepted
  pending this task).
- Note from the design plan: at 400% speed the emulator reaches ~75 FPS instead of 200 —
  profiling must find the bottleneck. Plan: `doc/PROFILING-OPTIMIZATION-PLAN.md`.

## Task 59: BIFROST still has tiny problem in Next mode

- Only happens when selecting --machine next
- Does NOT happen when using 48, 128 or plus3 modes (so BIFROST in this modes is correct)
- Similar to before, but only on column 20, the attribute is shown 1 scanline early
- Is this supposed to happen? Maybe BIFROST is not ready for next and cannot be assumed to work in Next mode. Investigate because this may be a non-issue, and just say that Bifrost games MUST be run in one of the working modes.
- Are the bifrost/nirvana tests defined only for 48 mode, or also for 128 and plus3? Since the contention is different on each, we should extend the tests to cover all 3 types.
- **Investigation CLOSED 2026-07-14 — hardware-consistent non-issue, docs-only, merged.**
  Verdict (a): under `--machine next` NextZXOS itself commits NR 0x03 = +3 timing; measured
  physics IDENTICAL to plus3 (228 T/line, 3.5 MHz, contention on), yet the contention-locked
  loop settles one 8-T octet earlier (locked hc 276 vs 292, both wobble-free) — the §9
  equilibrium-selection class from Task 52; jnext doesn't choose the equilibrium, the software
  environment's approach dynamics do. Only char column 18 (0-based; "20" 1-based) straddles its
  fetch boundary. Adjudicable only on real Next hardware. Evidence table:
  G12-NIRVANA-STATUS-2026-07-13.md **§10**.
  **Disposition: run BIFROST (and 48K multicolour engines) in 48k/128k/plus3 mode.**
- Test-extension answer: the 4 rows from Task 55 were 48k-only. **Extension DONE + MERGED
  (Task 59b, review waived by user)**: 8 rows ({4 screens} × {128k, plus3}), every reference
  FUSE-verified in the matching FUSE machine (bifrost exactly 0 px in the matching FLASH phase
  / exactly 248 px in the other = the 11 flash cells, both machines; nirvana/nirvanap static
  rows 0 px + identical colour-index sets). **No divergence found — the Task 54 128K/+3
  contention stretch tables survive their first FUSE lock.** Next mode deliberately NOT pinned
  (§10). BIFROST self-adapts to 128K timing (pixel-identical across 48k/128k/plus3 in both
  emulators, verified per machine, not assumed).

## Task completion status

- [x] Task 55: DONE — 4 tests, FUSE-verified refs, independent APPROVE, merged @ 2af14d35
- [x] Task 56: DONE — frame-edge 50/60 commit, independent APPROVE, merged @ 54486a72
- [x] Task 57: DONE — SD2 @ c7ef2dad, tape-save @ b0d40ae1, BOOT-DOT, DeciLoad @ 0b3c5e81 all merged (4 independent APPROVEs); mmu+nmi skips → 0
- [x] Task 58: DONE — Copper c_max_vc repush + NR 0x05 bits 2/0 frame-latched readback, independent APPROVE (zero findings), merged
- [ ] Task 13a: Next-specific snapshot schema (G66+G67) — PARKED
- [ ] Task 27: performance optimization
- [x] Task 59: DONE — investigation closed (equilibrium, G12 §10; BIFROST → 48k/128k/plus3) + 128k/plus3 extension merged (8 FUSE-locked refs)
