# Tasks

## Task 1: Rewrite older subsystem test suites in the Phase 2 per-row idiom

Bring **all 9** older subsystem test suites in line with the Task 5 Step 5 Phase 2 per-row `check(plan_id, ...)` / `skip(plan_id, reason)` idiom so that `doc/testing/TRACEABILITY-MATRIX.md` can report per-row status for every non-Z80N compliance suite. Today those suites report `—` in the Status column because they use data-driven loops, helper functions, or batched assertions instead of one assertion per plan row. Full coherence across all 15 compliance suites (everything except Z80N) is the goal — partial coverage of the idiom drifts back into theatre habits over time.

**Process**: same as Task 5 Step 5 Phase 2. Worktree-isolated author agents + independent critic reviewers (author ≠ reviewer per CLAUDE.md). Main session does build + test + commit. Three parallel waves of 3.

- **Wave 1 (biggest/most-active)**: MMU (143 plan rows), DMA (156), Audio (197).
- **Wave 2 (medium, known real bugs)**: NextREG (66), UART+I2C/RTC (105), DivMMC+SPI (124).
- **Wave 3 (smaller / low aggregate fail count)**: CTC+Interrupts (150), Tilemap (69), ULA Video (122).

**Explicit exclusion**: **Z80N** stays as-is. The FUSE-style data-driven runner (`test/z80n_test.cpp` parsing `tests.in`/`tests.expected`) is the correct pattern for opcode compliance testing — per-row IDs across 1356 near-identical rows would add noise without adding traceability. The aggregate pass count is the right signal; the plan's row list is the coverage inventory. Leave this suite alone.

**Special note on Tilemap**: the current suite contains "anti-tests" (e.g. TM-CB5) that pin the control-bit swap emulator bug as expected behaviour. The Wave 3 rewrite must **replace them with VHDL-correct assertions that FAIL against the current emulator**, same pattern as Layer2 G7 in the Phase 2 Wave 1 rewrite. This cleanly decouples the test refactor (Task 1) from the emulator fix (Task 2 item 8): once Task 1 lands, Task 2 item 8 becomes a one-liner in `Tilemap::set_control` that flips N tests from fail to pass. Do NOT also apply the emulator fix in the Task 1 Tilemap branch — keep the concerns separated.

**Special note on ULA Video**: the single existing failure (S13.14 frame_done T-state flip, Task 2 item 4) stays as a failing `check()` after the refactor — same rationale as above. Do not fix it in Task 1.

**Rules specific to this task**:

- Plans already exist at `doc/testing/*-TEST-PLAN-DESIGN.md` and are VHDL-derived — do NOT rebuild the plans, only the test code. This is a test-code refactor, not a plan rebuild.
- Every plan row must map to exactly one test row: a `check(plan_id, ...)` if the facility is exercised in the current C++, or a `skip(plan_id, reason)` if it is not. No data-driven loops without per-iteration IDs. No helper-function call sites that aggregate multiple plan rows into one assertion.
- **No emulator fixes in this task**. Emulator bugs surfaced by the refactor go to Task 2 (the carried-over Task 3 backlog). Tests are the specification; leave them failing.
- The refactor is expected to be mechanical **except** for 10-20% of rows per subsystem where plan/test/emulator disagree — same (A)/(B)/(C)/(D) triage as Phase 2: test harness bug, test expected-value bug, legit emulator bug, or plan bug. Fix (A)/(B) on the branch, leave (C) failing, note (D) without editing the plan.
- Critic reviewers apply the §4 checklist in `doc/testing/UNIT-TEST-PLAN-EXECUTION.md`: VHDL fidelity sampling, failure classification, stub honesty check, coverage count, tautology sweep, test-ID discipline.
- After each subsystem merges: update the plan's **Current status** block, the `EMULATOR-DESIGN-PLAN.md` subsystem row, and the corresponding section of `doc/testing/TRACEABILITY-MATRIX.md` (per §6a of UNIT-TEST-PLAN-EXECUTION). The matrix diff should land on the same branch as the refactor.
- Lint must remain clean (`baseline: 2  new: 0`).
- User must run `bash test/regression.sh` on real desktop after the merges land — sandbox has no graphics substrate.

**Expected outcome**: all 9 `—` cells in the TRACEABILITY-MATRIX summary's Status column flip to concrete `pass`/`fail`/`skip` numbers. Honest pass/fail counts become visible per row across ~1132 additional plan rows (sum of the 9 subsystems). Task 2 backlog may grow if the triage surfaces new real emulator bugs — fold them into the right item in Task 2 rather than creating new ones.

**Not scheduled here**: only Z80N is excluded, for the reason stated above. Every other compliance suite gets rewritten this session.

Reference: `memory/project_older_test_rewrites_planned.md` has the full rationale and wave plan; `doc/testing/UNIT-TEST-PLAN-EXECUTION.md` has the authoring/review/matrix-refresh process; `memory/project_task5_step5_phase2_complete.md` has the Phase 2 outcome to cross-check results against.


---

## Task 2: Fix emulator baseline — failing unit tests (carried over verbatim from 2026-04-14 Task 3)

> This task is the Task 3 section from `.prompts/2026-04-14.md`, carried forward verbatim. It lists all ~20 emulator bugs the unit test plan phase surfaced. Work sub-item by sub-item, one branch per fix, author + critic per CLAUDE.md, `bash test/regression.sh` on real desktop before merge. Do NOT touch the tests unless they are wrong — tests are the specification.

### Task 2 scope and approach

- Work on Phase 9, task "Fix baseline of subsystem tests not passing"
- Start agents in parallel for each subsystem, up to 3 simultaneous agents
- Use the VHDL and C++ expert skills as usual
- Ensure code review of changes, as indicated in CLAUDE.md
- Do not touch the tests unless authorized. We need the tests to be the source of truth.

### Confirmed real bugs surfaced during Task 5 Step 2 (2026-04-14)

Each item below was empirically verified during Step 2 test-fix work. Tests and VHDL agree; the emulator is wrong. Each has a fix sketch but NONE have been applied (user directive: emulator fixes go to Task 3, not Step 2). Addresses and citations below refer to the repo state at commit `f0b5fa0`.

1. **I2C false STOP detection** — `src/peripheral/i2c.cpp:101`. `detect_start_stop()` is called unconditionally from `write_scl()` but uses `prev_sda_` which was last updated by `write_sda()`. On the first bit of every `i2c_send_byte`, the sequence `write_sda(1)` (prev_sda_←0, sda_out_←1) followed by `write_scl(1)` causes the STOP check `scl==1 && prev_sda_==0 && sda_out_==1` to fire, resetting state to IDLE before the slave can sample. Per I2C spec, START/STOP are SDA transitions — detection should only run from `write_sda`, never from `write_scl`. Fix: remove the `detect_start_stop()` call at line 101. Collapses all 9 UART/I2C baseline failures (I2C-P03, I2C-P05a/b, RTC-01/02/04/05/06/07).

2. **AY envelope hold-at-is_bot off-by-one — wider than shape 0/9** — `src/audio/ay_chip.cpp` envelope state machine. Per VHDL `ym2149.vhd` L411-462 the down+hold branch when alt=0 should latch hold on `is_bot` (env_vol==0), not `is_bot_p1` (env_vol==1) — a one-step-too-early hold trigger. **Scope widened by Wave 1 Audio rewrite (2026-04-15)**: failing tests are AY-81, AY-102, AY-110, AY-122 (not just the originally-identified shape 0/9). AY-113 previously thought wrong is actually VHDL-correct at YM=0x01 (plan wording "hold at 0" was the incorrect one, not the emulator). Investigate all envelope shapes that exercise the down+hold (alt=0) branch.

3. **Turbosound default panning and pan=01 routing** — audit flagged TS-10 ("Reset: all panning to both L+R") as a real emulator bug; Step 2 empirical run also shows TS-42 ("Pan 01: R only, L silenced") broken with `L=0 R=0`. Both point to the Turbosound/YM2149 mixer not initializing panning to `0b11` (both channels) at reset AND the pan register 01 decode being wrong. Likely a single missing init in the Turbosound class reset path plus a decode off-by-one.

4. **ULA frame completion timing** — `test/ula/ula_test.cpp` S13.14 "Frame complete after full T-states" shows `frame_done=0 after 69888 T-states`. S13.01 and S13.02 confirm the emulator's `hc_max()*vc_max()/2 = 69888` (matches VHDL), and S13.03 now asserts this honestly. The bug is that `frame_done` doesn't flip when the T-state counter reaches the frame boundary. Investigate the T-state accumulation path in the ULA/video-timing class.

5. **MMU reset slot clobber** — `Mmu::reset()` sets `slots_[0]=0x00, slots_[1]=0x01` but VHDL `zxnext.vhd:4611-4612` explicitly assigns `MMU0 <= X"FF"; MMU1 <= X"FF"` on reset (0xFF = ROM sentinel). Fix: change the two reset assignments. Risk: boot-path code may assume concrete bank numbers at startup; if so, fix the boot code to respect the ROM sentinel. Failing tests: RST-01, RST-02 in `mmu_test.cpp`. This was originally pre-approved as a cheap Step 2 emulator fix but deferred per the "emulator bugs → Task 3" rule.

6. **DMA zero-length transfer** — `test/dma/dma_test.cpp` rows 9.8, 14.6, 14.7 (Wave 1 2026-04-15 refactor IDs — was 9.7/14.4 pre-refactor) show the DMA transfers 1 byte when `block_len==0`. Root cause in `src/peripheral/dma.cpp:560`: counter post-increment before the length check means the first byte always transfers regardless of `block_len`. VHDL `dma.vhd:426` compares `counter < block_len` before the transfer → FINISH_DMA with 0 bytes when both are 0. Fix: move the length check before the byte transfer.

6a. **DMA byte-mode (R4 mode=00) transfers whole block instead of one byte** — `test/dma/dma_test.cpp` row 12.6 "Byte mode: one byte then stop (block_len=4)" currently transfers all 4 bytes. VHDL `dma.vhd:601` defines mode `00` as single-byte-then-stop. **NEW Task 2 backlog item from Wave 1 2026-04-15**. Likely a missing mode-dispatch in the transfer loop.

6b. **DMA continuous+prescaler gap** — `test/dma/dma_test.cpp` row 12.7. `src/peripheral/dma.cpp:581` guards `burst_wait_` with `mode_ == 2` (burst only), so continuous mode (`mode_==1`) never enters prescaler wait. VHDL `dma.vhd:424` enters WAITING_CYCLES whenever the prescaler is nonzero, regardless of R4 mode. **NEW Task 2 backlog item from Wave 1 2026-04-15**. Fix: decouple the prescaler wait-arm from the mode check.

7. **NextREG reset-defaults tests rewrite as integration tests** — 14 RST-xx failures in `test/nextreg/nextreg_test.cpp` are a test-design issue, not an emulator bug. The bare `NextReg` class doesn't own the VHDL reset defaults (NR 0x12=0x08, 0x14=0xE3, 0x42=0x07, 0x4A=0xE3, 0x4B=0xE3, 0x50-0x57 MMU mirrors, 0x82-0x89 port enables). These defaults live in subsystem classes (Ula, Layer2, Mmu, port dispatcher) that hook handlers on NextReg. When the test constructs a bare `NextReg`, no subsystems are attached, so reads return zero. The real emulator is not broken — in a full `ZxNext` boot, port 0x253B reads return the correct VHDL values. **Fix direction**: rewrite these 14 tests as full-machine integration tests (same pattern as demo/regression tests): construct `ZxNext`, reset, read via port dispatcher, check against VHDL defaults. Do NOT duplicate state by moving defaults into `NextReg::reset()` — that would blur ownership. See `memory/project_nextreg_integration_test.md` for the full rationale.

8. **Tilemap control-bit swap** — from original Task 4 audit (not re-verified in Step 2 runs). `Tilemap::set_control` has bit 3 (text mode) and bit 5 (strip flags) swapped relative to VHDL `tilemap.vhd`. Several tests in `test/tilemap/tilemap_test.cpp` are currently "anti-tests" that document the bug as expected behaviour (TM-CB5 etc.). Fix: correct the bit mapping in the C++ class AND delete the anti-tests, replacing them with VHDL-correct assertions.

9. **Ram class default size is 768 KB, should be 1792 KB** — `src/memory/ram.h:7` has `explicit Ram(size_t size_bytes = 768 * 1024)`. The `Emulator` class (`src/core/emulator.h:285`) holds `Ram ram_` with no explicit size, so the running emulator only has 768 KB of usable RAM instead of the ZX Next spec 1792 KB (224 × 8K pages). Any NEX program declaring `ram_required = 1` (1792 KB) silently fails to fit. Surfaced during Task 5 Step 4 while fixing the MMU test fixture (which was also at 768 KB — those test fixtures were bumped to 1792 KB in the L2M-01 rewrite commit). Fix: change the default to `1792 * 1024` in `ram.h`. Verify: re-run all unit tests (they allocate explicitly, so no change expected) plus `bash test/regression.sh` and a NEX program that needs >768 KB to confirm the emulator actually uses the new space. Also audit `Ram::load_state` for any buffer-size mismatch when loading older snapshots.

### Additional items surfaced during Task 5 Step 5 Phase 2 (2026-04-15)

Step 5 Phase 2 rebuilt the test code for the six retracted "100%-pass" theatre suites (Layer2, Sprites, Copper, Compositor, I/O Port Dispatch, Input) from the new VHDL-derived plans. Each rewrite ran through an independent critic reviewer and landed on main at `c520af0`. The new honest pass rate is 445/496 live (89.7%) with 152 honest stubs/skips and 51 legitimate emulator-bug failures listed below. Regression passed on real desktop. Tests are the specification; the emulator is wrong — fix the C++, not the tests.

10. **Layer 2 `+1` bank transform missing.** `src/video/layer2.cpp:52-61` `compute_ram_addr()` computes `(active_bank + sub_bank) * 16384` but VHDL `layer2.vhd:172` specifies `layer2_bank_eff = (('0' & active_bank(6:4)) + 1) & active_bank(3:0)`. The code comment even documents the `+1` as intended-but-not-implemented. Breaks Layer2 G7-01/02/03, G7-05a/b/c (6 tests). Fix: apply the `+1` to the top 3 bits of NR 0x12 before combining with `sub_bank`. Any real NEX file that reads from L2 banks ≥ 1 currently reads garbage under strict bank checking.

11. **Sprites — Y clip is 8-bit, VHDL is 9-bit.** `clip_y1_`/`clip_y2_` in `src/video/sprites*` cannot address y=256; VHDL `y_e_v` is 9-bit. Breaks G4.XY-04. Widen the clip Y fields to 9 bits.

12. **Sprites — assorted missing facilities.** All of these block test coverage and collectively account for 5 of the 10 plan stubs:
    - `SpriteEngine::max_sprites_` exists but is never set → no per-line overtime detection; G13.OT-02/03/04 uncoverable.
    - NR 0x15 bit 5 `border_clip_en` not plumbed into the engine; G11.OB-03 uncoverable.
    - Non-over-border clip applies the X non-linear transform (`((v>>5)+1)<<5|v&0x1F`) to Y as well; VHDL only applies it to X. Needs a VHDL walk of the sprite clip process.
    - `mirror_inc_i` pulse not publicly observable; G1.AT-10 wrap is implicit instead of explicit.
    - Anchor-H 4-bit latch not publicly observable; G12.RP-03/04 uncoverable.

13. **Copper — four missing facilities.** 0 live failures today (stubs in the plan), but they block meaningful coverage:
    - NR 0x64 / cvc offset model entirely absent. No handler, no field, no readback. Plan Open Question #5. Blocks 6 OFS-* rows.
    - No cycle-accurate shared `nr_wr_*` bus — `Copper::execute()` calls `nextreg.write()` directly so simultaneous CPU+Copper contention at the 28 MHz clock isn't observable. Blocks ARB-01/02/03.
    - NMI `nmi_cu_02_we` signal path not modelled; blocks ARB-06.
    - `nr_copper_write_8` sticky latch per `zxnext.vhd:4883-4887` unmodelled. `Copper::write_reg_0x63` always takes the 16-bit path. Currently invisible under RAM-MIX-01 stimulus (coincidental match: `write_data_stored_` happens to yield the same value) but will bite other interleavings. Add a `write_8_` flag toggled on any NR 0x60 write and consulted by `write_reg_0x63`.

14. **Compositor — nine gaps in `src/video/renderer.cpp`.** Breaks 24 tests across TR, PRI, L2P, BL, STEN, UTB, LINE, BLANK groups. All VHDL-verified by the critic reviewer against `zxnext.vhd:7090-7356`:
    1. NR 0x14 palette-compare transparency absent — `renderer.cpp:136` `is_transparent` uses ARGB alpha=0 only. The VHDL compares the upper 8 bits of the 9-bit palette output against `transparent_rgb_2` for ULA/Tilemap-text/Layer2, and uses `pixel_en` for Sprites/Tilemap-non-text/Layer2-empty. Historical bug class.
    2. Blend modes 110/111 unimplemented — `renderer.cpp:259` falls back to SLU. VHDL 7288-7298 (additive clamp-to-7) and 7316-7321 (sub asymmetric).
    3. Stencil mode (NR 0x68 bit 0) absent — no state in `renderer.h`. VHDL 1125-1137.
    4. ULA/TM blend mode (NR 0x68 bits 6:5) absent — no `ula_blend_mode` state. VHDL 7141-7177 picks `ula_mix_rgb` or `ula_final_rgb` based on the mode.
    5. L2 palette bit 15 priority promotion absent — renderer's case 0/2/4/5 never tests `layer2_priority`. VHDL 7220/7242/7264/7276 promote L2 above the blend result.
    6. Border exception absent — no `ula_border_2 and tm_transparent and not sprite_transparent` clause in modes 011/100/101 at `renderer.cpp:240-256`. VHDL 7256/7266/7278.
    7. NR 0x15[0] global `sprite_en` gating absent at the compositor tier. Breaks TR-42.
    8. Per-line stage-0 latch for NR 0x15 / NR 0x14 absent — only NR 0x4A is snapshotted. Breaks LINE-10/11.
    9. Blank output visibility at the `composite_scanline` boundary absent. Breaks BLANK-11/12/13.

15. **I/O Port Dispatch — twelve gaps.** Breaks 18 tests. Some items (like PR-01) are already on the pre-existing Task 3 list; listing here for completeness:
    1. `register_handler` silently accepts overlapping `(mask,value)` ranges — the known read/write asymmetry bug in `src/port/port_dispatch.cpp`. Breaks PR-01.
    2. NR 0x82-0x85 port-enable bit gating completely absent; `src/core/nextreg.cpp:8` does `regs_.fill(0)` so defaults are 0 not 0xFF. Breaks NR82-01/03, NR85-03b, NR-DEF-01, NR-85-PK, NR-RST-01, BUS-86-01, LIBZ80-05.
    3. Pentagon 0xDFFD handler missing. Breaks REG-10.
    4. Kempston 1 (0x001F) and Kempston 2 (0x0037) read handlers missing. Breaks REG-18/19.
    5. Kempston mouse 0xFADF/FBDF/FFDF handlers missing. Breaks REG-20, REG-26/27.
    6. ULA+ 0xBF3B/FF3B handlers missing. Breaks REG-21.
    7. DivMMC NR 0x83 b0 enable gating not wired. Breaks AMAP-03.
    8. AY 0xFFFD gated read doesn't return floating-bus byte. Breaks BUS-02.
    9. SPI 0xE7/0xEB handlers not routed. Breaks REG-12 (the test-harness contribution was fixed during review; the genuine wiring gap remains).
    10. 128K bank latch 0x7FFD write not observable via dispatcher. Breaks REG-08.
    11. `PortDispatch` constructor pre-registers stub AY/MMU/NextREG handlers at `port_dispatch.cpp:4-27` that collide with real ones wired by `emulator.cpp:526/639/572` — structural one-hot violation.
    12. CTC range covers 0x183B..0x1B3B, should be 0x183B..0x1F3B. Breaks NR85-03b (top-of-range).

    Plan nit (not an emulator bug): IO Dispatch plan row REG-17 wording says "0x133B rejected" but VHDL line 2639 and `src/core/emulator.cpp:747` both route 0x133B to UART TX-status. The test correctly tracks observable VHDL. If the plan is ever re-edited, fix the wording.

16. **Input — `NextReg::reset()` zeroes the whole register file instead of applying signal-declaration defaults.** Two observable failures:
    - NR 0x05 should reset to 0x40 per `zxnext.vhd:1105-1106` + the soft-reset block at `:4920-4945` that intentionally does NOT clear `nr_05_joy*`. Breaks JMODE-08. This extends/supersedes the pre-existing `nr_05_joy reset` item tracked in `project_task3_new_items_2026_04_14.md`.
    - NR 0x0B should reset to 0x01 per `zxnext.vhd:4939-4941` + packing at `:5200-5203`. Breaks IOMODE-01. New item.
    - Fix direction: `NextReg::reset()` should apply the VHDL signal-decl defaults for every register that declares one, not `memset(0)`.

17. **Input — entire joystick subsystem missing from C++.** 126 plan rows are skipped as honest implementation debt. The missing pieces:
    - NR 0x05 decoder: joy0/joy1 mode mux per `zxnext.vhd:5157-5158` (`joy0 = D3 & D7 & D6`, `joy1 = D1 & D5 & D4`).
    - Kempston 1 port 0x1F and Kempston 2 port 0x37 read handlers, with `port_1f_hw_en` guard and bits 7:6 mask in Kempston mode vs pass in MD mode.
    - MD 3-button routing per mode 101 signal ordering `A C B U D L R` at bit positions 6..0.
    - MD 6-button state machine from `md6_joystick_connector_x2.vhd:66-193` plus NR 0xB2 read-handler, with no Kempston gating.
    - Sinclair 1/2 and Cursor joy→membrane adapters (joystick signals translate into fake key presses).
    - NR 0x0B user-I/O pin-7 mux + UART routing.
    - NR 0x06 NMI gating (hotkey + software NMI paths, bits 3/4).

18. **Input — Kempston mouse entirely missing.** Ports 0xFADF/0xFBDF/0xFFDF (X/Y/buttons), enable gate, L/R reverse adapter, wheel wrap, DPI setting. 11 test rows skipped.

19. **Input — keyboard matrix is only the base membrane.** Missing: CS hysteresis and `i_cancel_extended_entries`, extended-column folding per `membrane.vhd:237-240`, NR 0xB0/0xB1 extended-key matrix state + read-handler. Blocks the EXT-* and KBDHYS-* categories (23 rows).

20. **Input — port 0xFE byte-assembly wrapper missing.** The read-side composition of bit 7/5 constants, EAR bit 6, NR 0x08 issue-2/3 select, and the expansion-bus AND at `zxnext.vhd:3459/3468/5182` is not in `src/`. FE-01..05 + KBD-22/23 skipped.

### Additional items surfaced during Task 1 Wave 2 (2026-04-15)

21. **NextREG `selected_` reset default** — `src/port/nextreg.cpp` reset sets `selected_=0`, but VHDL `zxnext.vhd:4594-4596` specifies `nr_register <= X"24"` on reset. One-line fix; failing test: SEL-02 in `test/nextreg/nextreg_test.cpp`. **Does not duplicate state — this is `NextReg`'s own storage, so it is a direct fix, not an integration-test issue.** Distinct from Emulator Bug backlog item 7 which concerns subsystem-owned defaults.

22. **UART select-register read bit6 vs bit3** — `src/peripheral/uart.cpp:299` returns `(select_ ? 0x40 : 0x00) | msb` for the select-register read, but VHDL `uart.vhd:371` emits `"01000" & msb` → bit 3 (`0x08`) for UART 1. Fix: replace `0x40` with `0x08`. Failing tests: SEL-02, SEL-05, DUAL-02 in `test/uart/uart_test.cpp`.

23. **DivMMC `write_control` mapram bit is OR-latch in VHDL** — `src/peripheral/divmmc.cpp` `write_control(0x00)` clears `mapram_`, but VHDL `zxnext.vhd:4182-4183` OR-latches the mapram bit (once set, stays set except via a dedicated clear path). Failing test: E3-04.

24. **DivMMC `read_control` missing bit-5:4 mask** — `src/peripheral/divmmc.cpp` `read_control` returns the raw register byte; VHDL `zxnext.vhd:4190` masks bits 5:4 to zero on read-back. Failing tests: E3-07, E3-08.

25. **DivMMC ROM3 automap gating absent** — `DivMmc::check_automap` iterates `entry_points_0_` bits unconditionally without consulting `entry_valid_0_` / ROM3 state. VHDL `zxnext.vhd:2892-2905` requires `i_automap_rom3_active` for all entry points except EP0 (PC=0x0000), and the NR 0xBA/0xBB automap sources only match when ROM3 is current. Currently the C++ wrongly traps into DivMMC on any 0x0008/0x0038 RST fetch regardless of ROM context. Failing tests: EP-02, EP-03, EP-11, NR-01, NR-02, NR-05 in `test/divmmc/divmmc_test.cpp`. Fix requires plumbing `automap_rom3_active` from MMU into DivMmc and gating the decode.

26. **SpiMaster `write_cs` lacks decode / single-device collapse** — `src/peripheral/spi.cpp` `SpiMaster::write_cs` stores the raw byte; VHDL `zxnext.vhd:3311-3322` decodes the CPU data and collapses ambiguous or unrecognised values to `0xFF` (single-device enforcement). Failing tests: SS-09, SS-10, SS-11.

27. **SPI MISO pipeline delay not modelled** — VHDL `spi_master.vhd:162-166` latches `miso_dat` one cycle after `state_last`, so the first read after select returns the *previous* exchange result. C++ `SpiMaster::read_data` returns the current exchange result. See `memory/project_spi_vhdl_behavior.md`. Failing test: SX-03.

28. **SpiMaster reset does not initialize `ishift_r` to 0xFF** — VHDL `spi_master.vhd:151-152` resets `ishift_r` to `0xFF`; `src/peripheral/spi.cpp::reset()` leaves it at 0. Failing test: ML-05.

### Additional items surfaced during Task 1 Wave 3 (2026-04-15)

29. **Tilemap rotate swaps X/Y** — VHDL `tilemap.vhd:323-324` swaps the transformed X and Y when `rotate=1`; `src/video/tilemap.cpp` render path does not apply this. Failing test: TM-15.

30. **Tilemap 512-tile mode (control(1)=1) not implemented** — VHDL `tilemap.vhd:194` `nr_6b_tm_control(1)` enables 9-bit tile indices; VHDL `tilemap.vhd:393` maps `attr(0)` as tile bit 8 in this mode. `src/video/tilemap.cpp` ignores bit 1 of the control register and always uses 8-bit tile indices. Failing tests: TM-30, TM-31. Separate from but adjacent to the control-bit swap item 8.

31. **ULA Video `Ula::set_ula_enabled` bool not wired into render pipeline** — no failing test (demoted to skip as S12.02/S12.03 during review), but the C++ exposes `set_ula_enabled`/`ula_enabled` as a plain bool field that `render_scanline` never consults. VHDL `zxnext.vhd:5445` `nr_68_ula_en` should gate the rendered ULA output. Candidate Emulator Bug backlog item for a future cycle when the test harness is extended to observe pipeline output through `nr_68` bit 7. Low priority because §12.01 reset default is already the only real assertion on this accessor.

Wave 2 also surfaced many *skip*-class gaps (NMI button lifecycle, RETN hook, SPI state counter, MISO priority ladder, SRAM address ladder, automap_reset vs set_enabled, NR 0x09 bit 3 clear mapram, SD swap routing) — those are tracked as honest skips in `test/divmmc/divmmc_test.cpp` and are sub-features of item 25's broader "DivMMC surface is too thin" theme; address them piecemeal as each sub-feature lands.

### Approach for Task 2

- For each item, work on a separate branch off main. Create worktrees under `.claude/worktrees/`.
- Do build + test + commit work **from the main session**, not from delegated sub-agents. Worktree sub-agents hit a sandbox wall on Bash execution regardless of permission rules (see `memory/feedback_subagent_sandbox.md`).
- Each emulator fix gets an independent critic code-review agent per CLAUDE.md before merge.
- After each fix, run the full `bash test/regression.sh` on a real desktop env before merging — sandbox has no graphics substrate and reports spurious screenshot failures.
- Items 1, 2, 3, 5 are each expected to be ≤10-line emulator fixes.
- Item 4 (ULA frame completion) needs investigation — size unknown.
- Item 6 (DMA zero-length) likely small but test 9.7/14.4 imply a state-machine subtlety; may need a VHDL walk.
- Item 7 (NextREG) is the biggest: ~50-100 lines of fixture code plus rewriting 14 test bodies. May span multiple sessions.
- Item 8 (Tilemap) requires both emulator fix and deleting/rewriting anti-tests — treat as one batch.


## Task Completion Status

- [ ] Task 1: PENDING - Older subsystem test rewrites (all 9: MMU, DMA, Audio, NextREG, UART+I2C, DivMMC+SPI, CTC, Tilemap, ULA Video) in three parallel waves of 3
- [ ] Task 2: PENDING - Emulator baseline fixes, carried over from 2026-04-14 Task 3 (20 backlog items across 16 subsystems)
