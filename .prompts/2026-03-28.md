# Tasks

## Task 1: debugger enhancements

- I wrote new debugger requirements in Phase 7.5. Start work on that phase.
- Make the debugger window 50% wider. Things are starting to look packed
- Rename the "Step Into" button to "Single Step"
- When right click on immediate $15DE in ASM window, I get the option "Watch $15DE", but also "Watch (DE)". The second one is wrong, its detecting the DE register, but it's not, its an hex immediate
- Make the bottom Memory panel occupy 65% and the Watches panel 35% width
- Move the Debug toolbar to the bottom of the debugger window. Align he buttons to the right.
- Change keyboard shotcuts: F5 for Run / F7 Single Step / F8 Step Over / F9 Step out / F10 Pause
- Include the shortcut texts in the buttons, e.g. "F7 - Single Step"
- Make the bottom split (Memory/Watches) 60%/40%
- Remove the RUN/Pause buttons - Buttons: "F5: Continue"/"F9: Break" on the left, "F6: Single Step", "F7: Step Over","F8: Step Out" on the right. yes rework the Keyboard shotcuts again
- Rename Run/Pause options in menu to Run/Continue and Pause/Break
- I want to be able to Edit a watch from the Watch panel. Add a button and a dialog for doing so
- The Add/edit watch dialog is a bit small, make it 50% wider
- In tracing you need to add also AF' BC' DE? HL' IX IY and FLAGS to each trace line
- In the main emulator window, change the debug icon for one with a bug icon
- Change debugger menu bar order: Debug - Map - Breakpoints - Watches
- Add a "Breakpoints" panel which shows the active breakpoints, their type, and allow add/edit/remove. I don't know what layout, suggest me one and implement

## Task 2: create a Spectrum ROM map

- The Spectrum ROM map is here: https://skoolkid.github.io/rom/maps/all.html
- Process it and generate a simple map file with each address and subroutine name or variable, so that we can load it in the emulator and trace through the ROM with symbolic names
- Just use the subroutine names, clean them up. E.g.: "THE 'SKIP-OVER' SOUBROUTINE" should generate a symbol named "SKIP_OVER"
- For system variables, use the names verbatim
- Output the MAP file as "48rom.map"
- Add an option in the map menu to import the 48rom.map file format

## Task 3: official NextZXOS boot image debugging

- Ensure NextZXOS official image boots and works. It will help debug the peripheral implementation (SD card, etc.)

## Task Completion Status

- [x] Task 1: Phase 7.5 debugger enhancements implemented (all 13 items complete)
- [x] Task 2: 48rom.map generated (395 symbols), simple MAP format loader added
- [ ] Task 3:
