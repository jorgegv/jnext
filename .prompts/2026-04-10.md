# Tasks

## Task 1: Phase 8 - Backwards execution

- Work on Phase 8, backwards execution, using the previously generated plan for this feature
- Use the expert-zxspectrum and expert-zx-demoscene skills for this task
- Make the trace mode start activated if the rewind is activated too
- Add a "|< Frame Back" button
- When I run 1-frame back and forth in the debugger, the video panels change (as expected), but the emulator window shows different content. Keep them in sync.
- Can you modify the layer2_sprite_demo program so that instead of a cyan background, some simple but colorful pattern is shown on the Layer2?

## Task 2: prepare for public release

- When I run a build (`make release`), lots of warnings are shown. Fix them. I want NO warnings.
- Add a version.yaml file, make the standard bump-minor, bump-major, bump-patch and bump(->bump-minor) Makefile targets for managing it, and put embed the version in the About dialog (GUI) and in the startup, so that the version is output on the terminal as well. Add a --version CLI flag for the same.
- Create a FEATURES.md with the current emulator features. Simple one line description, and choose the ones most interesting for developers.
- Create a TODO.md with the pending features, again one per line.
- Put both files at the top directory

## Task Completion Status

- [x] Task 1: Phase 8 backwards execution
- [x] Task 2: prepare for public release (warnings fixed, version.yaml, FEATURES.md, TODO.md)