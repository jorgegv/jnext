# Tasks

## Task 1: Phase 8 - Backwards execution

- Work on Phase 8, backwards execution, using the previously generated plan for this feature
- Use the expert-zxspectrum and expert-zx-demoscene skills for this task
- Make the trace mode start activated if the rewind is activated too
- Add a "|< Frame Back" button
- When I run 1-frame back and forth in the debugger, the video panels change (as expected), but the emulator window shows different content. Keep them in sync.
- Can you modify the layer2_sprite_demo program so that instead of a cyan background, some simple but colorful pattern is shown on the Layer2?

## Task Completion Status

- [x] Task 1: Phase 8 backwards execution — snapshot infrastructure complete
  - `StateWriter`/`StateReader` serialisation classes in `src/core/saveable.h`
  - `save_state`/`load_state` on all subsystems (22 files: video, audio, cpu, peripheral, core)
  - `RewindBuffer` ring buffer in `src/debug/rewind_buffer.h/.cpp`
  - `Emulator::save_state`/`load_state` calling all subsystems in order
  - `replay_mode_` + `frame_num_` fields + snapshot taken at top of `run_frame()`
  - `EmulatorConfig::rewind_buffer_frames` (default 500) + `--rewind-buffer-size` CLI option
  - Fixed `PaletteManager::save_state` visibility (was private, moved to public)
  - All 22 regression tests pass
  - Snapshot size ~941 KB/frame; 500 frames ≈ 459 MB allocation at startup
- [x] Phase 3: Replay mode and rewind algorithm
  - `replay_mode_` gates audio mixing, video rendering, trace recording, and RZX/video capture
  - `Emulator::rewind_to_cycle(uint64_t)` — restore nearest snapshot + fast-forward to target
  - `Emulator::step_back(int n)` — uses TraceLog to find target cycle, clears stale trace, rewinds
  - `Emulator::rewind_to_frame(uint32_t)` — rewind to exact frame boundary
  - `StepMode::STEP_BACK` + `RUN_BACK_TO_CYCLE` added to DebugState
  - `DebugState::step_back(n)` / `run_back_to_cycle(target)` methods
  - Handled at top of `run_frame()` before snapshot
  - Fixed off-by-one in trace index: `trace_size - n` (not `trace_size - 1 - n`)
  - 5 unit tests in `test/rewind_test.cpp`: ring wrap, step_back PC, rewind_to_frame, round-trip, disabled
  - All 23 regression tests pass (rewind-func added)