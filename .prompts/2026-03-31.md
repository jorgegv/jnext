# Tasks

## Task 1: Add additional debugging features

- Add a CLI option to the emulator that waits for a given period and then outputs a PNG file with a screenshot of the current emulator screen. E.g. --delayed-screenshot <filename> and --delayed-screenshot-time <seconds>, with a default of 10 seconds
- Add another CLI option to do an automatic exit from the emulator after a given time: --delayed-automatic-exit <seconds>



## Task 2: official NextZXOS boot image debugging (continued)

- Reading the WAY-FORWARD document and its findings, recover your memories and continue debuggin2cg the NextZXOS issue
- You may use this approach:
  - Debug automatically until you solve the problem or I command you to stop
  - Compile with: `cmake --build build -j$(nproc) 2>&1 | tail -30`
  - Run always with full trace with: `./build/jnext --log-level=emulator=info,memory=debug,divmmc=debug --boot-rom roms/nextboot.rom --divmmc-rom roms/enNxtmmc.rom --sd-card roms/nextzxos-1gb-fat32fix.img 2>&1 | tee /tmp/jnext-boot.log`
  - You can check the trace log at `/tmp/jnext.log`. Feel free to change the name of the output log if you need to (see previous command)
  - Run AUTOMATICALLY the emulator for tests with a `timeout` command, but use the _kill_ option to it, to ensure the emulator is really stopped when you want to. It seems it currently ignores some signals.
  - Use the automatic screenshot and  automatic exit features developed above to take a timed screenshot of the emulator and compare it with a reference image. The reference image is a standard "(c) Sinclair Research Ltd." one that is shown when booting is completed successfully.
  - The reference image is `test/img/boot-reference.img`. It should not be modified or rewritten under any circumstances.
  - If you manage to boot the NextZXOS up to the point you get a screenshot identical to the reference one, you will have solved the bug.
  - Again, do not ask for building or testing confirmation, do it yourself with the commands provided.
- Feel free to add more tracing if you need, following the current conventions


## Task Completion Status

- [x] Task 1: Delayed screenshot + automatic exit CLI options added
- [ ] Task 2: NextZXOS boot — root cause fully analyzed, closest approach was config page write recording + soft_reset replay (96K across 6 banks replayed, but DivMMC automap reset needed on soft_reset). See memory for full details.