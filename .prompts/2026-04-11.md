# Tasks

## Task 1: Debug NEX games

- Debug celeste.nex game: the game test/nex/celeste.nex just shows a black screen on startup. Debug it with me.

## Task 2: bug fixes

- When running the tilemap demo in Zesarux the tiles are apparently defined correctly, but it does not scroll correctly, it's flashing back and forth between several images positions and only moves vertically. It should work in Zesarux first. Fix the demo so that it works in Zearux, and then let's fix jnext.
- z88dk CRT analysis:
  - The source code for some of the NEX games is available, see @NEX-TEST-PROGRAMS.md file
  - They are done with Z88dk, with CRT=31 (minimal)
  - The game author told me that no NextZXOS has been used in his games
  - Can you verify if NextZXOS is used in the z88dk CRT for the Next, even minimally?
  - z88dk source can be found in /home/jorgegv/src/spectrum/z88dk
- put each of the demo programs on its own directory, and fix the Makefile in the demos directory

## Task 3: add David's nexlib and tests

- I added a new Next library and some tests by David Crespo in directory demo/dapr-nexlib+tests
- Analyze the code, create any Makefile needed and add the tests to our regression test suite. The tests already work, so don't touch the code! 
- Ensure each test creates a NEX file for testing. I'll need to check them one by one in ZesarUX and manage a way to get a reference execution screenshot.
- Can you adapt the regression test suite to run tests in parallel?

## Task 4: Phase 9: verify and fix David Crespo's tests

- Work on Phase 9, on David Crespo's tests
- Test03: does not work (black screen), but when examining the sprite layer in the debugger, it looks the sprite has been defined and rotates correctly.
  - May be the layer priority is wrong?
  - In Zesarux the background is blue and fills also the border. In JNext the border is white and background black (like ULA). Fix it.
  - Ensure our NEX loader does the same setup as the original TBBLUE's NEXLOAD command


## Task Completion Status

- [x] Task 1: Debug celeste.nex — found and fixed 2 bugs, identified 1 blocker
  - **Fixed**: Tilemap base address decode (`tilemap.cpp:decode_base_addr`) had wrong bit extraction (`(reg_val >> 1)` instead of `reg_val & 0x3F`) and wrong multiplier (1024 instead of 256), verified against VHDL
  - **Fixed**: Tilemap reset defaults didn't match VHDL (map=0x2C/0x6C00, defs=0x0C/0x4C00)
  - **Fixed**: Updated tilemap demo to use correct register value (0x20 instead of 0x10)
  - **Blocker**: celeste.nex requires NextZXOS/esxDOS API (RST 08h / function 0x9A = M_P3DOS) to load game data from files. Without NextZXOS ROM support, the game crashes at address 0x80ED after ~11500 instructions. This is a missing emulator feature, not a bug.
- [x] Task 2: Tilemap demo scroll fix — X scroll regs 0x2F/0x30 were swapped vs VHDL; demo used sdcc_iy causing IY corruption by IM1 handler. Verified working in ZesarUX.
- [x] Task 3: README update — added missing CLI options (--inject, --boot-rom, --divmmc-rom), fixed help text default, updated FEATURES.md
- [x] Task 2 (z88dk CRT): CRT startup=31 does NOT use esxDOS/NextZXOS. But CelesteNext game code uses esxDOS (`persist.c`: `esxdos_f_open/read/write/close`) for save/load. Also fixed MMU page 0xFF → ROM mapping (was treated as RAM). Game still needs esxDOS/NextZXOS to run.
- [x] Task 3: Added dapr-nexlib+tests (12 tests), regression suite parallelized (42s vs 4.5min)
- [ ] Task 4: IN PROGRESS
  - **Fixed**: NEX loader now sets NextREG 0x15=0x01 (sprites visible, SLU) per nexload.asm — test03 sprite now shows
  - **Fixed**: NEX loader now initializes full nexload.asm-compatible register state (0x14, 0x4A, 0x4B, 0x12, 0x13, clip windows, etc.)
  - **Fixed**: Layer 2 transparency now compares palette RGB output vs global transparency colour (VHDL zxnext.vhd:7121), not raw pixel index — IMPORTANT: this fix is coded but test03 still shows black background, needs further debugging
  - **TODO**: test03 background should be blue (palette entry 0 = blue, transparency 0x00 = black, so non-matching). The transparency fix logic is correct per VHDL but the test still shows black. Test01/test02 L2 works, so memory path is correct. Need to investigate why blue doesn't show — possibly compositor or border issue
  - **TODO**: remaining dapr tests (04-12) not yet evaluated vs ZesarUX