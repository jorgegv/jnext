# Tasks — 2026-07-13

Baseline at the start of this file: **unit 4490 (4452 pass / 0 fail / 38 skip, 46 suites) ·
FUSE 1356/1356 · regression 60/0/0 · harness-selftest 26/26.**

**CURRENT (main @ 5c1a2cd2): unit 4559 (4535 pass / 0 fail / 24 skip, 47 suites) ·
FUSE 1356/1356 · regression 64/0/0 · harness-selftest 26/26.**

Merged this session: **31, 38, 41, 13b, 8a+8b, 43, 45, 46, 47, 48, 49, 50** (skips 38 → 24).
Nothing merged without an independent APPROVE. Task 50 was **REJECTED on first review** and
merged only after the defect was fixed.

> ## BIFROST/Nirvana do NOT work on `main`, and are not expected to.
> The multicolour effect needs **two** things and main has only one:
> 1. **The attribute mux (G12)** — replays attribute writes per scanline. Without it every cell
>    collapses to its LAST write of the frame and multicolour cannot render at all.
>    **Lives ONLY on `task8-nirvana`. NOT on main.**
> 2. **The contention fix (Task 50)** — makes the demo race the beam at the right time. **On main.**
>
> So: contention fix alone changes nothing visible for BIFROST. Both together take it from
> 9.7% → 4.3% pixel difference vs the FUSE oracle. Do not expect a correct BIFROST from a
> main-only build.

---

# OPEN

## Task 50: the contention window was displaced by 64 scanlines — DONE (merged)

**MERGED to main. Found while chasing Nirvana; it is NOT a Nirvana bug — it mis-timed every
contended 48K/128K/+3 program.**

**REJECTED on first review, and the reviewer was right:** `T50-01` was **theatre**. It probed the
top border at raw hc=160, where `hc_adj = (160 & 0xF) + 1 = 1` and `1 & 0xC == 0` — so the
HORIZONTAL phase gate returns 0 whatever vc says. The row reported "the border does not contend"
while actually measuring "this hc phase never contends", and **passed with the bug still present**.
Proven by mutating `to_ula_counters()` to identity: the other five T50 rows failed, T50-01 sailed
through. Probe moved to raw hc=120 (contends in BOTH coordinate systems), so only the vertical
border check can zero it. All six now fail under mutation. **The author had written the
mutation-test instruction into the review brief and had not run it on their own rows.**

`ContentionModel::contention_tick()` gates on `border_active_v`, transcribed correctly from
`zxula.vhd:414` (`i_vc(8) or (i_vc(7) and i_vc(6))` → "border when vc >= 192"):

```cpp
if ((hc & 0x100) != 0) return 0;               // src/memory/contention.cpp:255-258
if ((vc & 0x100) != 0) return 0;
if ((vc & 0xC0) == 0xC0) return 0;
```

The transcription is right; **the inputs are wrong.** In the VHDL, `i_hc`/`i_vc` are the ULA's
**display-relative** counters — `zxula_timing.vhd:441-452` resets `vc_ula` to 0 at `c_min_vactive`
(the first display line) and `hc_ula` at `ula_min_hactive` (`= c_min_hactive - 12`, `:423`). That
is *why* "vc >= 192 = border" is the correct test there. jnext feeds the gate the **raw frame
counters** from `derive_hc_vc()` (`src/cpu/z80_cpu.cpp`), where vc=0 is the frame start and the
display only begins at vc=64. So the window sits 64 lines too high:

- contention **is applied across the top border** (raw vc 0-63) — a real Spectrum applies none;
- contention **is not applied over the bottom 64 display lines** (raw vc 192-255) — it should be.

The two errors partially cancel over a frame, which is why no screenshot regression caught it.

**Measured against real FUSE** (headless — see the FUSE oracle recipe below) on bifrost.tap.
Same PC, same code path, INT-relative T-state of the write to `$5865`:

| | jnext before | jnext fixed | FUSE (oracle) |
|---|---|---|---|
| contention T charged since INT | 1118 | **290** | ~310 (implied) |
| INT-relative T of the write | ~20492 | **~19716** | **19684** |

**~870 T of phantom contention per frame → +32 T.** A ~96% reduction, independently re-measured
by the reviewer via gdb on the `tstates` global (FUSE cycles 19684, 19908, … +224/phase over 8
phases; jnext post-fix 19716, 19940, … — the **same** 8-phase cycle).

**The +32 T residual is a CONSTANT bias on every phase — NOT jitter.** I originally called it
"within INT-acceptance jitter"; the reviewer measured it and there is no jitter, it is a fixed
offset. See Task 52.

**Fix (implemented):** rebase raw (hc, vc) onto the ULA counters before the gate, per machine,
from `VideoTiming` (`ula_prefetch_origin_hc()`, `display_origin().vc`) — pushed into the CPU via
the new `z80_set_ula_counter_origins()`. **No hardcoded 48K constants.**

**Nine existing tests were passing BECAUSE of the bug** — this is the part worth remembering:
- `CT-INT-01`, `CT-DELAY-01`, `CT-FUSE-01/02/05` ran their timing programs from `tstates = 0`
  (raw vc 0 = **top border**) and asserted contention fires there.
- Four `cpu_z80n_im2_regressions` rows seeded `tstates = 2` with the comment *"hc=4, vc=0,
  **active raster**"*. They meant ULA coordinates and got raw ones for free.

All were corrected to express their *same intended raster position* properly, not to go green.
Beware one trap: the IntSmoke program reads every 20 T (= 40 ticks ≡ 0 mod 8), so its contention
phase is **locked** by wherever it starts — whatever bin of the VHDL wait pattern
`{6,5,4,3,2,1,0,0}` the first read lands in, every read lands in. **Do not hunt for a start offset
that produces a nonzero result** (that is the Nirvana arm-heuristic anti-pattern). The helper
anchors on the ULA counter origin `(hc_ula, vc_ula) = (0,0)` — the hardware's own window opening —
which is defensible without looking at the outcome.

**New test rows: `T50-01..06`** in `contention_test` (89 → 95, manifest updated deliberately).
They drive the **production seam** — set the FUSE T-state counter to a raster position, call the
real CPU memory callback, measure what it charges — instead of calling `contention_tick()` through
its front door with pre-cooked coordinates. That seam is what was never tested: the 68 existing
rows pass ULA-relative coords, production passed raw ones, and nothing checked the two agreed.

**Outcome:** regression **64/0/0 — ZERO reference screenshots moved**, despite this changing memory
timing for all contended software. That is the two halves of the bug (over-contending the top border,
under-contending the bottom third) cancelling over a whole frame — exactly why it survived so long.

## Task 52: jnext runs a constant +32 T-states behind FUSE per frame (NEW — 2026-07-13)

Surfaced by the Task 50 review, which re-measured the fix independently instead of trusting the
author's number — and found the residual is **not** what the author claimed.

After Task 50, jnext writes `$5865` in bifrost.tap at INT-relative T 19716 where FUSE writes it at
19684: a **constant +32 T offset on every one of the 8 phases** (19716/19940/… vs 19684/19908/…).
It is a **fixed bias, not jitter** — the author's "within INT-acceptance jitter" was wrong, and the
reviewer proved it by gdb'ing the `tstates` global across phases.

32 T is ~1/7 of a scanline, so it is not currently visible; BIFROST renders correctly. But it is a
real, reproducible divergence from the oracle and it will matter to anything racing the beam at
sub-scanline precision. **Do not fold this into Task 50** — the contention gate is now VHDL-correct
(the reviewer verified the rebase against `zxnext.vhd:4444-4445` / `:6737-6738` independently).
Likely candidates: the tape-trap path, or the INT-acceptance edge (jnext accepts with 0 latency at a
raster position that jitters 51-131 T while FUSE's is fixed). Needs its own measurement, own task.

**Method:** the FUSE oracle recipe below + gdb on jnext's `tstates` global, breaking in
`fuse_z80_writebyte` conditioned on the address. Both sides give the same 8-phase cycle, so the
comparison is exact rather than statistical.

## Task 51: video timing is never re-pushed on a runtime NR 0x03 change (NEW — 2026-07-13)

Found while doing Task 50; deliberately kept out of its scope.

`video_timing_.init()` is called **once**, at `Emulator::init()` (`emulator.cpp:379`). So do
`z80_set_contention_runtime()` (which sets `s_tstates_per_line` / `s_tstates_per_frame`) and the
new `z80_set_ula_counter_origins()`. A **runtime NR 0x03 machine-timing write** re-fans-out
`machine_48_or_p3_` to `im2_` and `cpu_` (`emulator.cpp:2489-2497`) and the `MachineTimingMode` to
the contention shadow — but **not** the video timing constants. After such a write the CPU-side
`derive_hc_vc()` keeps deriving (hc, vc) with the *previous* machine's line length and origins.

Relevant because the NextZXOS boot path **does** write NR 0x03. Needs its own task + review.

## Task 8 / Nirvana (G12-MUX) — PARKED at `task8-nirvana` @ `97d62dbf`, NOT merged

**FULL WRITE-UP: [doc/issues/g12-nirvana/G12-NIRVANA-STATUS-2026-07-13.md](../doc/issues/g12-nirvana/G12-NIRVANA-STATUS-2026-07-13.md)**
(conclusions, evidence PNGs incl. the FUSE oracle). That doc is still the place to start — but the
"open hypothesis" section of it is now **answered**, see below.

**Root cause found — it was Task 50, not the mux.** The parked WIP commit records the detail:

- **The T-state write-timestamp hypothesis was TESTED and REFUTED as the cause.** It is a real bug
  (the scanline tag came from the `on_scanline()` event, refreshed only between instructions, so any
  write inside an instruction that straddles a line boundary is tagged a whole scanline early) — but
  it affects only **0.71%** of BIFROST's attribute writes and moved the pixel diff by **3 px**. The
  fix (`attr_mux_set_write_pos`, tag from the write's own T-state) is a keeper and sits on the branch.
- **The real cause was the contention window (Task 50).** jnext charged BIFROST ~808 phantom
  T-states per frame in the top border, so the demo raced the beam ~4 scanlines late, every frame.
- With **both** the mux and the Task 50 fix, BIFROST goes **4787 → 2125 differing px vs the FUSE
  oracle (9.7% → 4.3%)**, with no fudge factor. (A `-4` scanline bias hack had only reached 2350 —
  the real fix beats the hack, which is how you know it is the real fix.)

**Settled — do not re-litigate:** no gate / always on (user decision, the gate bought ~0%); the arm
heuristic was **hiding** a bug, not optimising; the ULA bank-selection bug (Timex mode vs the 7FFD
shadow bit) is fixed; **the beast.nex reference was CORRECT — never regenerate it.**

**Next, in order:** land Task 50 → rebase the Nirvana branch on it → strip the throwaway probes and
the hardcoded `to_ula_counters` experiment from the branch → re-measure BIFROST and Nirvana → chase
the residual 4.3% if it is still there.

## Task 8 — remaining `mmu_test` skips (12)

- **BOOT-DECI-01..04 (4)** — TZX 0x15 DeciLoad + WAV real-time (G36/G37). The two documented open
  Pending Issues in `EMULATOR-DESIGN-PLAN.md` §11. Known-hard; wants a dedicated session.
- **BOOT-TAPESAVE-01..03 (3)** — tape save (G33). A whole feature, not a skip to tidy away.
- **G12-MUX-01..03 (3)** — the Nirvana work above.
- **SD2-01/02 (2)** — Soundrive Mode 2 paging suppression (G146). **The most tractable next target.**

## Task 13a: Next-specific snapshot schema (G66+G67) — PARKED

`.szx` structurally cannot carry Next state. This is the real answer for it.

## Task 27: performance optimization — NOT STARTED

User: *"it's huge."* Also the agreed home for the G12 mux's ~5-10% cost.

## Task 48: the debugger's audio mute checkboxes do nothing

- `src/debugger/audio_panel.{h,cpp}` — the five source-mute checkboxes are **decorative**: zero
  `connect(...)` anywhere, nothing reads `isChecked()`.
- Same family as Task 41 (that panel decoded NextREG fields that do not exist). The panel had **no
  test coverage at all** until Task 41 added its first six rows — which is why both survived.
- Fix: wire them to a real per-source mute in the mixer (composing with `--silent`), or remove them.

## Task 49: add `--delayed-automatic-exit-frames`

Exit time in frames, mirroring `--delayed-screenshot-frames`. Trivial.

## ULA clip window (NR 0x1A) — the last open per-line-deferral item

VHDL has a stage-0 register (`:988-991`); `apply_ula_clip` reads the live members with no snapshot.
Not yet given a task number. (The other five `renderer.h` per-line arrays are all genuinely wired —
see the closed inventory below.)

---

# REFERENCE — method knowledge earned this session

## FUSE as a headless, scriptable oracle (NEW — this is a big one)

FUSE renders BIFROST correctly and **ZEsarUX does not**, so FUSE is the oracle. It can be driven
**headless and scripted**, which turns "compare screenshots by eye" into "compare T-states":

```bash
export DISPLAY=:78
Xvfb :78 -screen 0 1024x768x24 &          # X11 — xdotool/import work in here; Wayland they do NOT
printf 'break write 0x5865\ncommands 1\nprint ula:tstates\ncontinue\nend\ncontinue\n' > dbg.txt
GDK_BACKEND=x11 timeout 25s /usr/bin/fuse --machine 48 --no-sound --auto-load --traps \
    --debugger-command "$(cat dbg.txt)" --tape test/00regression/tap/bifrost.tap
```

- `--debugger-command` is **not in `--help`** — it is in `man fuse`. It is the only way to feed
  multi-line debugger commands.
- `break write ADDR` / `break ADDR` / `break port read|write P`, `commands ID … end` (auto-run on
  hit), `print EXPR`, `continue`. Numbers print in **hex**.
- **`ula:tstates` = T-states since the last interrupt** — exactly the frame of reference a
  beam-racing demo works in, and directly comparable to jnext's own INT-relative counter.
- `import -window root out.png` under Xvfb captures the screen (this is how the oracle PNG and a
  repeatable oracle generator were obtained).

This is what turned the Nirvana investigation from guesswork into measurement: same PC, same code
path, one number each side, difference = 808 T.

## Diagnostic discipline (paid for in real time)

- **A demo passing is not proof the model is correct.** Nirvana "looked right" for two rounds while
  the model was wrong. **BIFROST is the discriminating demo** — that is why all three TAPs exist.
- **Do not tune a constant until the tests go green.** The Nirvana arm heuristic and (nearly) the
  Task 50 test anchor were both this trap. *A heuristic tuned until the tests stop failing is
  indistinguishable from a bug fix, right up until someone removes the heuristic.*
- **Test the hypothesis before fixing it.** The T-state write-timestamp theory was plausible,
  self-consistent, and explained every observation — and was worth **3 pixels**. One probe
  (0.71% of writes mis-tagged) killed it in one build.
- **When a fix makes existing tests fail, ask whether they were asserting the bug.** Nine were.
- **Crop to the 256×192 display area before pixel-diffing** (jnext writes 640×512, FUSE's window
  was 1276×957); point-sample only (`-sample`, never an interpolating resize — it invents colours);
  calibrate alignment on something the feature does not touch (the plain-ULA text), never on the
  thing under test.
- Demo NEX/TAP runs take `--machine 48k|next --load FILE` and **no `--sdcard`**.
- The user's clipboard screenshots: `wl-paste --type image/png > out.png`.

---

# DONE this session

- **Task 31** — compositor stencil mode ignored the ULA enable (VHDL `:7130`).
- **Task 43 / 45 / 46 — the per-line deferral bug class, now CLOSED.** Six instances across four
  tasks: `ula_en` (31), stencil + blend (43), NR 0x14 for ULA/TM (45), NR 0x14 for **Layer 2** (46).
  All five `renderer.h` per-line arrays are genuinely wired. **46 was the only one users could see:**
  Layer 2 **skip-writes**, so a wrongly-transparent pixel was *destroyed* and the compositor could
  not recover it.
- **Task 13b — standard snapshot formats** (`.z80` loader; `.szx` / `.nex` savers). **Reopened once
  and redone.** Every `.szx` jnext produced was **unreadable by real FUSE** (libspectrum rejects RAM
  pages > 63; we wrote 112) and our own round-trip tests could not see it because **saver and loader
  shared the blind spot**. Now machine-scoped: 48K saves `{5,2,0}`, 128K/+2A/+3 `{0..7}`, correct
  `chMachineId`, and **Next REFUSES to save** rather than lying in the header.
  → **Standing rule: validate a file-format saver against a FOREIGN reader.**
- **Task 47 — `--silent`** (~8-11% throughput win). Review caught it writing a **corrupt 262-byte
  MP4 while logging success** under `--silent --record`.
- **Task 38** — `make clean` silently reconfigured `build/` with `ENABLE_QT_UI=OFF`.
- **Task 41** — debugger audio panel decoded a PSG stereo field that does not exist.
- **Task 8a + 8b** — SKIP reduction (38 → 24).
- Repo cleanup: 245 worktrees / 57 GB freed, 305 branches deleted, `roms/` dead weight (~1.07 GB).
  `CLAUDE.md`'s `@`-refs now point at `doc/design/` and `doc/analysis/` where the files actually are.
- **NOT A BUG** (the backlog entry was itself wrong): `print_usage()` does not lie about `.rzx`.
  *Do not fix a bug that is not there.*

## Report-only sweep of other render-time state (from Task 45)

Answers the "per-scanline display state replay" bullet in EMULATOR-DESIGN-PLAN — most of it is
**already closed** via per-subsystem change-logs, contrary to what that bullet implies:

| State | VHDL pipelines it? | jnext status |
|---|---|---|
| NR 0x15 layer priority | yes (`:6799,6880,7081`) | **closed** — `nr15_change_log_` (G02) |
| NR 0x6B tilemap enable | yes | **closed** — `apply_nr6b_changes_for_line` (G06) |
| Port 0xFF Timex mode | yes (`:6796`) | **closed** — `ula_.rewind_to_baseline()` (G07) |
| L2 X/Y scroll (NR 0x16/0x17/0x71) | into the L2 sub-module | **closed** — L2 scroll change-log |
| **ULA clip window (NR 0x1A)** | **yes** (`:988-991`, stage-0 reg) | **OPEN** — reads live members |
| NR 0x70 (L2 res / palette offset) | into the L2 sub-module | **unverified** — needs its own look |

The register-state axis is nearly done. The genuinely missing mechanism for Nirvana-class work is
the *memory-write* axis (G12), which is untouched by any of this.

---

## Task completion status

One line per task. Details in each task's section above.

- [x] Task 31: compositor stencil mode ignores the ULA enable (VHDL 7130)
- [~] Task 8: 8a + 8b done. **Nirvana (G12-MUX) PARKED** on `task8-nirvana` @ `97d62dbf`, NOT merged
- [x] Task 13b: standard snapshot formats (.z80 loader, .szx/.nex savers) — reopened once, redone
- [ ] Task 13a: Next-specific snapshot schema (G66+G67) — PARKED
- [ ] Task 27: performance optimization — NOT STARTED (user: "it's huge")
- [x] Task 38: `make clean` silently reconfigures build/ with ENABLE_QT_UI=OFF
- [x] Task 41: audio panel decodes a PSG stereo field that does not exist
- [x] Task 43: two per-line NR 0x68 snapshots are dead code (stencil + blend)
- [x] Task 45: NR 0x14 transparency is the third dead per-line snapshot
- [x] Task 46: Layer 2 reads NR 0x14 live AND skip-writes
- [x] Task 47: add --silent option
- [x] Task 48: debugger audio mute checkboxes now actually mute (reviewed APPROVE, merged)
- [x] Task 49: --delayed-automatic-exit-frames (reviewed APPROVE, merged)
- [x] Task 50: contention window displaced 64 scanlines — REJECTED once (T50-01 theatre), fixed, merged
- [ ] Task 51: video timing never re-pushed on a runtime NR 0x03 change (NEW)
- [ ] Task 52: constant +32 T-state bias vs the FUSE oracle (NEW — found by the Task 50 review)
- [ ] ULA clip window (NR 0x1A) — last open per-line-deferral item, unnumbered
