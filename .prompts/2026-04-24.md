# Tasks

Notes: Today's plan:

First of all: read section "Post-Input plan follow-ups (backlog, not blocking)" and try to fix all backlogged issues.

Primary — Task (a) SKIP reduction (Task 3 here), ordered by boot-impact potential

Secondary — Task (d) fallback (Task 12 here): RE the enNextZX.rom:0x018E exit condition, only if nothing in (a) unblocks boot.


## Task 3: Work on pending features (test cases with SKIP result)

- Work on project plan Phase 9, section  "Reduce SKIPs — implement missing features"
- Work by subsystems. Don't go to the next one until one is finished, or I tell you so
- All work should be done by the usual expert: C++ plus VHDL
- Launch parallel waves of up to 5 simultaneous agents, one agent per feature
- Each feature in its own branch
- Allow agents to communicate if they have conflicts over the same code sections
- After each feature is developed, launch a new agent for reviewwing the work
- This task shall not be considered closed until I tell you so
- Work on the following subsystems (in order):
  - UART+I2C (48 skips)
  - Audio (73 skips)
  - Copper (1 skip)

## Task 12: NextZXOS boot — follow-on from Task 12c

Task 12 (MMU slot 6/7 alias, root cause) and Task 12c (architectural +0x20
shift via `Mmu::to_sram_page`) both landed.  NextZXOS boot still doesn't
complete — post-RESET_SOFT enNextZX.rom runs through RAM test + peripheral
init and then settles in DivMMC IM1 handler with blue-stripes VRAM (00/39).
Top suspect is the `RST 0x20` path inside enNextZX.rom.  Not a blocker; track
in the investigation journal and re-attempt after Task 3 MMU + NextREG SKIPs
land.

Use ultrathink mode for nextzxos boot investigation.

Full history, hypotheses and commits in
[doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md](../doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md).

### Update 2026-04-21 post-DivMMC+SPI + CTC+Interrupts landings

After the DivMMC+SPI and CTC+Interrupts skip-reduction plans both landed
on main today, a NextZXOS boot attempt shows big improvements: the
startup screen now shows some garbled attributes, but **ALL ROMs load
successfully apparently** (they display their log on the main screen)
and the machine **boots to 48K Spectrum mode** (apparently).  This is
a major step forward from the previous blue-stripes-in-DivMMC-IM1
terminal state.  Next-session investigation should:

- Verify the "48K Spectrum mode" observation (confirm screen MMU
  layout, identify which ROM path took over).
- Diagnose the garbled attribute area on the startup screen.
- Identify what's preventing the handoff to the NextZXOS desktop
  (expected) instead of dropping to 48K mode.
- Update `doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md` with the new
  fingerprint and hypotheses.

## Task Completion Status

- [x] Task 3 — ULA Video plan CLOSED end-to-end (Phases 0→4) + 3 post-closure amendments. `ula_test` 123/48/0/75 → **81/81/0/0** (ZERO skips). New `ula_integration_test` 7/7/0/0. Aggregate **3184/3041/0/143** (23 suites). 29 F-skips re-homed to 5 new subsystem plan docs (floating-bus, contention-model, compositor-NR68, videotiming-expansion, mmu-shadow). Full audit in `doc/testing/audits/task3-ula-phase4.md`.
- [x] **Session-handover step 1 — 3 new test-plan-design docs landed on main 2026-04-24** via 3 parallel worktree agents + per-branch critic + per-branch fix agent + per-branch re-critic:
  - `FLOATING-BUS-TEST-PLAN-DESIGN.md` — **26 rows** (5 re-home + 21 neighbours). Option (b): widened to cover port 0xFF on all machines AND port 0x0FFD on +3. Flags existing `Emulator::floating_bus_read` as buggy vs `zxnext.vhd:4513`.
  - `VIDEOTIMING-TEST-PLAN-DESIGN.md` — **22 rows** (7 re-home + 15 neighbours). V1 decision: VHDL-faithful `vc_max_` / `hc_max_` rebase; Section 1 + Section 6 coupled under new `F-VT-MAX-REBASE` skip reason.
  - `CONTENTION-TEST-PLAN-DESIGN.md` — **68 rows** (trimmed from 82 after theatre audit). Phased **A=28 / B=36 / C=4**. Surfaced that `ContentionModel` class already exists at `src/memory/contention.h`; refreshed stale `TASK-CONTENTION-MODEL-PLAN.md` in same branch. C2=move committed in plan text.
  - Merge commits on main: `980b6a5`, `7f8f7d2`, `aec8a2b`. `EMULATOR-DESIGN-PLAN.md` Phase 9 updated at `22cfc0c`.
- [x] **Session-handover step 2 — 3+2 re-home rows appended to Compositor + MMU plans 2026-04-24** in main session. `COMPOSITOR-TEST-PLAN-DESIGN.md` gets new **Group UDIS** (UDIS-01/02/03, re-home from ULA §12); `MEMORY-MMU-TEST-PLAN-DESIGN.md` gets **P7F-16/17** in Category 3 (re-home from ULA §15). Commits `ba7dbb5` + critic nit fix `30303d0`.
- [x] **Session-handover step 3 — 3 executable suites scaffolded 2026-04-24** via 3 parallel worktree agents + per-branch critic. All 3 critics APPROVED (1 applied 2 nits, 1 clean, 1 clean). Merged as `980b6a5` (floating_bus_test, 26 skip), `56755fa` (videotiming_test, 22 skip), `1f8d10b` (contention_test, 68 skip phased A=28/B=36/C=4). Two trivial Makefile/CMakeLists merge conflicts auto-resolved (same-line TESTS list appends). Aggregate after scaffolding: **3300/3041/0/259 across 26 suites**.
- [x] **Session-handover step 4 — UDIS-01/02/03 + P7F-16/17 appended 2026-04-24** to `compositor_test` (reopens 114/0/0 → 117/114/0/3) and `mmu_test` (reopens 148/0/0 → 150/148/0/2). Commit `9c82f5e`.
- [x] **Session-handover step 5 — final aggregate 3305/3041/0/264 across 26 suites** (exactly the predicted sum). Regression clean 34/0/0. No src/ touches, no plan drift, no test regression. All 5 handover steps complete.
- [x] **Task 3 UART+I2C plan AUTHORED** 2026-04-24 — `doc/design/TASK3-UART-I2C-SKIP-REDUCTION-PLAN.md` (commit `22ab4d2`, 725 lines, Phase 0→4). Q2 IM2 vector map independently VHDL-verified AGREE (4 vectors: 1=U0 RX, 2=U1 RX, 12=U0 TX, 13=U1 TX; rx_near_full OR baked into vectors 1/2, not separate).
- [x] **Task 3 UART+I2C Phase 0 MERGED** — re-home 12 cross-subsystem rows + 6 empirical flips (RTC-06..10 + I2C-P06, post-174fa56 false-STOP unblock) + Q2 vector-map correction. `uart_test` 106/58/0/48 → **94/64/0/30**. Plan-doc staleness refreshed.
- [x] **Task 3 UART+I2C Phase 1 scaffold MERGED** — bit-level `UartTxEngine` + `UartRxEngine` (7+8 VHDL states each) + RX FIFO widened to uint16_t + `I2cRtc` expansion (regs_ 8→64, osc_halt/mode_12h/use_real_time/poke/peek) + `I2cController::set_pi_i2c1_scl/sda` + `Emulator::inject_pi_i2c1`. 840+/-46 lines, 5 files. Default `set_bitlevel_mode(false)` — existing passes untouched. Critic APPROVE-WITH-FIXES → 2 bugs fixed in `3556a01` (RX edge XOR order, TX reset+break combo).
- [x] **Task 3 UART+I2C Phase 2 all 5 waves MERGED** — A (8 TX flips), B (6 RX flips + status bit 5 FIFO 9th-bit wiring), C (10 new uart_integration rows + Emulator port-enable gate wrap), D (2 new DUAL rows + `Emulator::inject_joy_uart_rx`), E (8 RTC + I2C-11 flips + I2cRtc transfer-path extension). Per-wave critics surfaced 2 more Phase 1 scaffold bugs: **TX parity XOR uses post-shift LSB** + **RX sticky-err orphaned on error path**. Fixed in `487928c`. Then `b86de83` flipped TX-09/10/13/14 + RX-08/09. Final: `uart_test` **101/92/0/9**, new `uart_integration_test` **12/12/0/0**, aggregate **3312/3087/0/225 across 27 suites**, regression **34/0/0**. 4 plan-drifts caught during Phase 0/2 (12-fail staleness, vector numbering, NR 0x82 vs NR 0x83 gate register, RTC-06/07 BCD-fix unnecessary).
- [ ] **Task 3 UART+I2C Phase 2 retrospective critic + Phase 3 + Phase 4** — DEFERRED to next session. Scope: full-sweep retrospective critic, audit the 9 residual `uart_test` skips for WONT/re-home decisions, refresh plan-doc Current status to live 101/92/0/9, write `doc/testing/audits/task3-uart-i2c-phase4.md`, update EMULATOR-DESIGN-PLAN.md Phase 9 UART+I2C to `[~]`/`[x]`, update SUBSYSTEM-TESTS-STATUS + TRACEABILITY-MATRIX.
- [~] Task 12: NextZXOS boot — not retried this session. Worth another boot attempt now that ULA scroll + ULA+/ULAnext + UART + RTC paths all more complete end-to-end.

## Session-handover scope — DO IN NEXT SESSION

User direction EOD 2026-04-23: handover, next session will build the test scaffolding for the 5 new subsystem plans before moving on to UART+I2C.

Order:

1. **Create 3 new test-plan-design docs** at `doc/testing/`:
   - `FLOATING-BUS-TEST-PLAN-DESIGN.md` (5 rows from `TASK-FLOATING-BUS-PLAN.md`).
   - `CONTENTION-MODEL-TEST-PLAN-DESIGN.md` (12 rows from `TASK-CONTENTION-MODEL-PLAN.md`).
   - `VIDEOTIMING-TEST-PLAN-DESIGN.md` (7 rows from `TASK-VIDEOTIMING-EXPANSION-PLAN.md`).
2. **Update 2 existing test-plan-design docs** — add re-homed sections:
   - `COMPOSITOR-TEST-PLAN-DESIGN.md` — NR 0x68 blend-mode (3 rows: S12.02/03/04).
   - `MEMORY-MMU-TEST-PLAN-DESIGN.md` — shadow-screen routing (2 rows: S15.03/04).
3. **Create 3 new executable suites** with skip() scaffolding (implementation doesn't exist yet):
   - `test/floating_bus/floating_bus_test.cpp` (5 skip rows).
   - `test/contention/contention_test.cpp` (12 skip rows).
   - `test/videotiming/videotiming_test.cpp` (7 skip rows).
   Plus `test/CMakeLists.txt` + `Makefile` wiring. Mirror `ula_integration_test` / `ctc_interrupts_test` template.
4. **Append skip() rows to existing suites**:
   - `test/compositor/compositor_test.cpp` — 3 new skip rows (reopens 114/0/0 → 117/0/3).
   - `test/mmu/mmu_test.cpp` — 2 new skip rows (reopens 148/0/0 → 150/0/2).
5. Expected aggregate after: **~3213/3041/0/172** across **26 suites** (same counts as before re-home, but every skip now in its owning subsystem).
6. Proposed: 3 parallel agents (one per new executable suite), each delivers design doc + test file + build wiring in one commit. Critic per branch, merge sequentially. Compositor + MMU updates in main session.

After that scaffolding lands, continue per the Primary ordering in this prompt file: **Task 3 UART+I2C (48 skips)** — also unblocks the 6 Input F-skips and lays groundwork for the ESP-01 AT-emulation feature item below.

## Post-Input plan follow-ups (backlog, not blocking)

- [x] **INPUT-TEST-PLAN-DESIGN.md §3.7 SINC1/2 keymap swap** — plan text has the keymaps inverted vs the authoritative `ram/init/keyjoy_64_6.coe` (and VHDL + FUSE). Test rows are correct; plan text needs editing to match COE. **Done 2026-04-23** (commit `95ce571`): §1.1 mode-code table, §3.7 rows, and Open Q §6.3 now match the COE + `membrane_stick.vhd:124-131`. The `zxnext.vhd:3429-3438` comment block is documented as stale.
- [x] **INPUT-TEST-PLAN-DESIGN.md EXT-18/19 fold-row labels** — VHDL `membrane.vhd:236-240` puts `,` in row 7 and LEFT in row 3 (plan said row 5 and row 7 respectively). Tests correctly assert no fold into the plan-named rows. **Done 2026-04-23** (commit `95ce571`): EXT-18/19 rewritten as explicit negative-test rows with the correct fold-destination citations; misleading test check() messages in `input_test.cpp` updated in the same commit.
- [x] **MembraneStick runtime wiring (core only)** — `compose_into_row()` is test-only; hook into `Keyboard::read_rows` so Sinclair/Cursor joystick games work once the host layer calls `Joystick::set_joy_left/right()`. GUI feeder split into "Joystick GUI" below. **Done 2026-04-23** (merge commit of `fc101dd` + `47a5751`): `Keyboard` gains a null-safe `MembraneStick*` back-pointer set once from `Emulator`'s ctor; `Keyboard::read_rows` applies `compose_into_row()` per-row AFTER extended-key folding; `Keyboard::reset()` documents the lifetime-bound invariant (must NOT clear the back-pointer). Bare-`Keyboard` unit tests preserved via nullptr guard. Critic APPROVE-WITH-FIXES → comment polish landed.
- [x] **Md6ConnectorX2 production tick (core only)** — `tick(master_cycles)` not called from the emulator frame loop; wire it alongside the existing CTC/UART ticks for runtime MD6 6-button support. GUI feeder split into "Joystick GUI" below. **Done 2026-04-23** (merge commit of `ec55f04` + `698fd9d`): `md6_.tick(master_cycles)` added at [emulator.cpp:2287](src/core/emulator.cpp#L2287) in the 28 MHz per-instruction cluster alongside CTC/UART. Critic surfaced a pre-existing prescaler-domain bug (`kCyclesPerClkEn = 16` was CPU-cycle units; `master_cycles` is 28 MHz) — fixed in the same branch to `128` per VHDL `md6_joystick_connector_x2.vhd:33` (~4.57µs @ 28 MHz = 128 cycles). FSM would have advanced 8× too fast at runtime without this.
- [ ] **Joystick GUI feature** (new backlog item 2026-04-23) — the host-side feeder: SDL/Qt gamepad event source (or keyboard-as-joystick fallback), per-connector joystick-mode menu (Kempston / Sinclair 1 / Sinclair 2 / Cursor / MD3 / MD6 / I/O), config-file persistence, and plumbing into `Joystick::set_joy_left/right()` + `Md6ConnectorX2::set_raw_left/right()`. Needs its own design doc before implementation because input-source choice + UX are non-trivial. Un-blocks real-world joystick-using games end-to-end.
- [ ] **NMI source pipeline** — Agent I landed only the NR 0x06 AND-gate; the full rising-edge/stackless/MF-return pipeline is still the fragmented NMI subsystem. Best tackled via a dedicated staged plan doc (`doc/design/TASK-NMI-SOURCE-PIPELINE-PLAN.md`) — 8 distinct VHDL sources across Copper + CTC + DivMMC + Multiface; ~6-7 skipped test rows unblock.
- [x] **NR 0x68 bit 3 → ulap_en wiring gap** — surfaced by ULA Phase 3 critic (2026-04-23). VHDL `zxnext.vhd:4550-4551` latches `port_ff3b_ulap_en <= nr_wr_dat(3)` when NR 0x68 is written, but jnext's NR 0x68 handler at `src/core/emulator.cpp:~762-768` doesn't wire bit 3 (only 0, 2, 7 are forwarded). **Done 2026-04-23** (merge of `a1495ba`): handler extended with `set_ulap_en((v & 0x08) != 0)`; new regression row `INT-ULAPLUS-02` in `ula_integration_test` clamps `ulap_mode=00` to gate off the port-0xFF3B path and exercises the ungated NR 0x68 latch via 0x00→0x08→0x00 transitions. Independent critic APPROVE. `ula_integration_test` 6/6/0/0 → **7/7/0/0**, aggregate **3216/3044/0/172** (23 suites), regression 34/0/0.
- [ ] **VideoTiming pulse-counter production wiring** — surfaced by ULA Wave E critic (2026-04-23). The new `VideoTiming::set_interrupt_enable`/`set_line_interrupt_enable`/`set_line_interrupt_target` + pulse counters are test-observable only; the Emulator scheduler still reads local `line_int_enabled_`/`line_int_value_` fields directly. **Status after 2026-04-23 audit: purely academic, no user-visible impact.** ULA frame + line interrupts already fire correctly in production via the existing scheduler (emulator.cpp:2138, :2154). No production code reads the VideoTiming pulse counters — they exist only for unit-test observability. Fixing means unifying the two state stores (drop the local fields, route scheduler through `VideoTiming`), a ~60-line refactor touching the interrupt-timing hot path — regression risk on screenshot tests that depend on exact interrupt cadence. Deserves its own plan doc + scoped waves if/when a future phase actually needs VideoTiming as a production API; low priority until then.
- [ ] **ESP-01 AT-command emulation (WiFi feature)** — new feature idea 2026-04-23. Splice an `Esp8266` single-instance subsystem into the Next's UART 1 TX/RX path and implement the Espressif AT command set at byte level (~15-20 commands for 90% of apps: `AT`, `AT+CWMODE`, `AT+CWJAP` fake-success, `AT+CIFSR` fake IP, `AT+CIPDOMAIN` real DNS, `AT+CIPSTART`/`CIPSEND`/`CIPCLOSE`/`CIPMUX`, `AT+CIPMODE` passthrough, async `+IPD` push from host sockets). Fake the WiFi layer, pipe sockets through the host network. MVP ~2-3 weeks; full-featured ~1 month. Precedent: CSpect already does this, so real-world apps are hardened against a simulated ESP. **Dependencies**: must follow UART+I2C plan (the 17 TX/RX + 6 INT + 3 GATE skips must close first — async `+IPD` needs reliable RX FIFO + interrupt routing). **Non-negotiable**: CLI sandbox flag (`--wifi=off|loopback|lan|internet`) + deny-list per host subnet. Needs its own design doc (`doc/design/TASK-ESP01-AT-EMULATION-PLAN.md`) before code lands.
