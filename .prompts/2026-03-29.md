# Tasks

## Task 1: official NextZXOS boot image debugging

- Ensure NextZXOS official image boots and works. It will help debug the peripheral implementation (SD card, etc.)
- Investigation journal: [doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md](../doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md).
 
## Task 2: more debugger enhancements

- Add "F2: Trace On/Off" button, and a Red/Green ball icon indicating Red=off, Green=on if Tracing is active
- Add "F3: Export Trace" button
- Reorganize the bottom toolbar: Tracing: (ball) | Trace On/off | Export Trace | --> | Continue | Single Step | Step Over | Step Out | Break |
- Integrate the red/green ball into the "F2:Trace" button and remove the trace state from the left
- The same way as the ASM panel is grayed out while executing normally, the registers panel should also not be updated unless in Single Step mode.

## Task 3: rewrite all tooling scripts in Bash

- I don't want Python scripts. Rewrite them all in Bash or perl
- Test the scripts!

## Task 4: sd card image contents

- Investigate what the SD card needs to have, but don't modify ghe IMG file!
- If we need to modify it, we will create a script to do it

## Task Completion Status

- [x] Task 1: SPI model rewritten (split read/write matching ZesarUX, CS deselect reset, command abort, CMD1, NCR bytes). Boot ROM works fully. BLOCKER: post-boot firmware reads MBR+BPB correctly but retries 3x — issue is NOT SPI. See [doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md](../doc/issues/NEXTZXOS-BOOT-INVESTIGATION.md).
- [x] Task 2: Debugger trace toolbar — F2 toggle, F3 export, indicator ball, toolbar reorganized
- [x] Task 3: Python scripts rewritten in Bash/Perl, tested identical output, Python files removed
- [x] Task 4: SD image analysis complete — CSpect image has all files except config.ini. Created fix-sdcard-image.sh to inject default config.ini. However, the "Error opening menu.ini/.def" is NOT caused by missing config.ini — firmware stops reading SD card after MBR+BPB. Root cause is likely a missing emulator feature needed by post-boot firmware code
