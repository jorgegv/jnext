# Tasks — 2026-07-16

Baseline at the start of this file: **main @ 835bcf8d · ctest 49/49 (4689/4689/0/0) ·
FUSE 1356/1356 · regression 86/0/0.**
End-of-session: **main @ 4b80d840 (+25 vs origin, UNPUSHED) · unit 4728/4728/0/0 (50 suites) ·
FUSE 1356/1356 · harness-selftest 26/26 · regression 87/0/0.** Tags v0.98.6…v0.98.9.

Continues `.prompts/2026-07-15.md`. Session state in memory:
`project_session_handover_2026-07-16_overnight.md`.

**Tonight's plan (user):** work Tasks **66 → 67 → 69** autonomously via the full merge protocol
(branch + worktree, green triplet, independent review, merge, `make bump-patch`, no push). Done.

## Housekeeping (2026-07-16, user-directed — not Phase 9 tasks)

- **Email purge (history rewrite).** The packaging work had written `j.g.villalonga@gmail.com` into 5
  FILES (CMake CPack contact/maintainer, `packaging/debian/{control,changelog,copyright}`,
  `packaging/rpm/jnext.spec`) — NOT into any commit author (git config was already `zx@jogv.es`).
  `git filter-branch --tree-filter` over `origin/main..HEAD` replaced it with `zx@jogv.es`; affected
  tags re-pointed (v0.98.7→abf1dab0, v0.98.8→69975b8e; v0.98.6 unchanged); refs/original dropped,
  reflogs expired, `git gc --prune=now`. **Verified 0 occurrences across the whole object DB.** Nothing
  pushed. ⚠ History was rewritten — a future push is fine (all commits were local-only).

## Task 63: investigate github issue 9 — OPEN, NEEDS MORE INVESTIGATION

Fix MERGED `7d1fa9dc`, bumped **v0.98.4**, independently reviewed APPROVE. 60 Hz demos: frontend
pacing now follows `Emulator::frame_period_ms()`. **danboid retested (2026-07-16): NOT fully resolved —
needs more investigation.** The pacing fix was necessary but not sufficient on his (weak, CPU-bound)
box; likely he still can't sustain 58–60 fps for the 60 Hz demo even after pacing + the ~300%
optimization. Next: profile/repro the residual on a constrained CPU budget. Details in
`.prompts/2026-07-15.md` Task 63.

## Task 64: investigate github issue 10 — CLOSED ✓

Fix MERGED `6557cb06`, bumped **v0.98.5**, independently reviewed APPROVE (2 review rounds).
NEXTEST.NEX = NextZXOS streaming app, not loadable via `--load`; `NexLoader` now rejects extended
NEX with a clear message. **danboid confirmed resolved (2026-07-16) — CLOSED.** (GitHub issue #10 may
already be closed by the reporter; verify/close if still open.)

## Task 65: Phase 9 — Optimization (pending issues)

Ref: [EMULATOR-DESIGN-PLAN.md](../doc/design/EMULATOR-DESIGN-PLAN.md) §"Phase 9 - CI, Quality and
Release" → "General optimization plan". Architecture assessment (Task 27a) and performance
profiling/optimization (Task 27, ~300%) are DONE. PENDING:
- General code refactor / tidy-up (`/simplify`), subsystem by subsystem then global.
- Magic-number → named-constant replacement where possible.
- The copper-demo/beast own-400% tail (needs a cycle-accurate/core change, not hot-spot tuning).

## Task 66: Phase 9 — Configurability — DONE (v0.98.6, merge 0491795c)

A user-facing configuration facility for the Qt GUI.
- **`src/gui/app_config.{h,cpp}` — `AppConfig` store.** Persists to `~/.config/JNEXT/jnext.conf`
  (QSettings INI, `config_version=1`). A second ctor takes an explicit INI path (unit-testable).
- **`src/gui/preferences_dialog.{h,cpp}` — Preferences dialog** (Settings → Preferences…), Startup +
  Paths tabs, OK/Cancel/Apply. Persists: machine type, CPU speed, emulator speed %, window scale, CRT
  filter, start-muted, tape fast-load; last-load-dir, default SD image, screenshot dir.
- **Load on start with CLI precedence.** Saved settings applied at GUI launch; **CLI flags always win**
  (`merge_cli_precedence`). **Headless never reads the config** (test determinism); `regression.sh`
  exports a throwaway `XDG_CONFIG_HOME`. Every default equals today's hardcoded behaviour (no config
  file ⇒ identical behaviour).
- Unit `app_config_test` (39 rows); suite pin 49→50. Independently reviewed APPROVE (coverage
  mutation-proven; determinism hazard reproduced).
- **Omitted / follow-ups:** Input tab (no runtime keyboard/joystick remap engine — separate Phase 11
  "redefinable keys"); MAP-dir (lives in DebuggerManager). Non-blocking: Machine-menu checkmark not
  synced after changing type via Preferences (cosmetic, pre-existing class); round-trip test only
  exercises +3 for the machine-type key.

## Task 67: Phase 9 — Packaging — DONE (v0.98.7, merge de4cd57b)

Distribution machinery.
- **CMake `install()` (GNUInstallDirs) + `include(CPack)`** (TGZ/DEB/RPM) in root CMakeLists.txt.
- **`packaging/assets/io.github.zxjogv.jnext.{desktop,metainfo.xml,svg,png}`** — self-made rainbow-"N"
  icon; `.desktop` (`Exec=jnext %f`, validated) + AppStream metainfo (validated).
- **Native packaging:** `packaging/rpm/jnext.spec` (Fedora), `packaging/debian/` (Debian/Ubuntu),
  `packaging/flatpak/…yml` (org.kde.Platform runtime), `packaging/make-dist-tarball.sh` (vendors
  submodules), `packaging/README.md` (maintainer guide).
- **CI:** `.github/workflows/packaging.yml` (Linux/Windows/macOS matrix).
- **Verified on this Fedora host:** TGZ/DEB/RPM all produced; full `rpmbuild -bb` succeeded; install-tree
  correct. Deps derived from `ldd`×`find_package` (ffmpeg is a `system()` shell-out ⇒ Recommends, not
  linked). Independently reviewed APPROVE (no findings).
- **Unverified (no runner):** Windows/macOS builds = CI config only; Flatpak `flatpak-builder` (manifest
  YAML-valid, SDL2 placeholder sha256). App-id `io.github.zxjogv.jnext` (noted adjustable).

## Task 68: Phase 9 — Documentation

Ref: [EMULATOR-DESIGN-PLAN.md](../doc/design/EMULATOR-DESIGN-PLAN.md) §"Phase 9" → "Documentation".
- DEVELOPMENT doc + process (architecture, subsystems, mermaid diagrams, issue/PR templates, tooling).
- User man page (README.md + USAGE.md already done).

## Task 69: Phase 9 — Integration test plan / CI — DONE (v0.98.8, merge cc41e833)

- **`doc/testing/INTEGRATION-TEST-PLAN.md`** — unifying doc: three-layer model (unit → integration →
  golden regression), a table of all 11 integration suites (subsystems spanned, scenarios, row counts),
  and an honest "gaps/future" section (DMA bus arbitration, multi-layer composition, full boot chain,
  RZX/rewind — none at the integration tier yet).
- **`.github/workflows/ci.yml`** — build + FUSE + unit + golden-screenshot regression on push/PR;
  provisions the SD image via jnext's own `--sdcard-download-confirm`.
- **`.github/workflows/release.yml`** — publishes a GitHub Release from `packaging.yml` artifacts on `v*`.
- **README badges** — CI status, latest release, GPLv3, C++17, Linux platform.
- **1 REJECT round:** reviewer empirically proved the CI-provisioned *pristine* SD image ≠ the
  `boot-nextzxos-dotls` reference (AE=15489 — that row renders a live SD `.ls` directory listing, and the
  local fixture was contaminated with dev-session files). Fixed at merge time by a workflow-level filter
  excluding `dotls`; re-reviewed APPROVE. **This exclusion was then made obsolete by the follow-up ↓.**

### Task 69 follow-up: SD-card self-provisioning — DONE (v0.98.9, merge 0b9b562c)

Root-fix for the `dotls` contamination, per user design.
- `regression.sh` `SD_CARD_ARGS` now EMPTY → every test uses jnext's **pristine fallback image**
  (`~/.jnext/sdcard/cspect-next-1gb-fixed.img`) instead of the maintainer's contaminated local
  `roms/nextzxos-1gb-fat32fix.img`.
- New counted **`[sdcard-provision]`** row runs after lint, before the parallel launch: downloads the
  image once via `jnext --headless --sdcard-download-confirm` if absent (idempotent, fails-loud).
- Regenerated **only** the `boot-nextzxos-dotls` reference against the pristine listing (22→17 files —
  reviewer confirmed exactly one PNG changed, content-driven, not a rendering regression).
- `ci.yml` simplified to run the **full unfiltered** suite; the Task-69 dotls exclusion is gone.
- Harness witness = `1 lint + 1 provision + screenshots + functional`; `harness-selftest 26/26`; regression
  87/0/0. Independently reviewed APPROVE (no findings). **The dotls repo-hygiene FLAG is now RESOLVED.**

## Task completion status

- [~] Task 63 — issue #9 (60 Hz pacing): fixed v0.98.4, reviewed APPROVE; OPEN — danboid says needs more investigation.
- [x] Task 64 — issue #10 (extended-NEX reject): fixed v0.98.5, reviewed APPROVE; CLOSED (danboid confirmed).
- [ ] Task 65 — Phase 9 Optimization: refactor/tidy-up, magic-numbers, copper/beast 400% tail.
- [x] Task 66 — Phase 9 Configurability: AppConfig + Preferences dialog, MERGED v0.98.6, reviewed APPROVE.
- [~] Task 67 — Phase 9 Packaging: install()+CPack+spec/debian/flatpak+CI, MERGED v0.98.7, reviewed APPROVE. - MacOS pending. Flatpak pending review.
- [ ] Task 68 — Phase 9 Documentation: DEVELOPMENT doc/process + man page.
- [x] Task 69 — Phase 9 Integration test plan + CI + badges, MERGED v0.98.8, reviewed APPROVE (1 REJECT round).
- [x] Task 69 follow-up — SD-card self-provision (dotls root-fix), MERGED v0.98.9, reviewed APPROVE.
- [x] Housekeeping — email purge (history rewrite, verified 0 in object DB); nothing pushed.
