# Tasks — 2026-07-17

Baseline at the start of this file: **main @ 8f0abc1f (v0.98.29) · pushed (origin/main = HEAD) ·
tree clean · ctest 50/50 (4732/4732/0/0)* · FUSE 1356/1356 · regression 90/0/0*.**
(*Task-70 branch-verified; main HEAD not freshly re-stamped — the only post-Task-70 change was
docs/Makefile/shell, native-C++-inert. Run `/test-triplet` for a fresh stamp before the next release.)

Continues `.prompts/2026-07-16.md`. Session state in memory:
`project_session_handover_2026-07-17_eod.md` (v0.98.26→.29 continuation of the 07-16 work).

## Migrated open tasks

### Release verification (07-17 EOD priority) — DONE ✓
- **v0.98.29 GitHub Release confirmed published** (user verified in GH). The >3-tags-in-one-push
  webhook caveat did not block it.

### Task 63: github issue #9 — 60 Hz demo on weak/CPU-bound box — OPEN
Frontend pacing now follows `Emulator::frame_period_ms()` (fixed v0.98.4, reviewed APPROVE). danboid
retested (2026-07-16): **NOT fully resolved** — the pacing fix was necessary but not sufficient on his
constrained CPU. Likely still can't sustain 58–60 fps for the 60 Hz demo even after pacing + the
~300% optimization. Next: profile/repro the residual under a constrained CPU budget. Details in
`.prompts/2026-07-15.md` Task 63.

### Task 65: Phase 9 — Optimization (pending)
Architecture assessment (Task 27a) and perf profiling/optimization (Task 27, ~300%) DONE. PENDING:
- General code refactor / tidy-up (`/simplify`), subsystem by subsystem then global.
- Magic-number → named-constant replacement where possible.
- The copper-demo/beast own-400% tail (needs a cycle-accurate/core change, not hot-spot tuning).

### Task 67: Phase 9 — Packaging — macOS leg UNVERIFIED
Linux TGZ/DEB/RPM + Flatpak + Windows all verified/published. **macOS = CI config only**
(`release.yml` macos leg is `continue-on-error`; fails on `#include <SDL2/SDL.h>` Homebrew layout).
No macOS runner on the dev host, so this stays unverified until a Mac is available.

### Task 68: Phase 9 — Documentation (pending)
Ref EMULATOR-DESIGN-PLAN.md §"Phase 9" → "Documentation".
- DEVELOPMENT doc + process (architecture, subsystems, mermaid diagrams, issue/PR templates, tooling).
- User man page (README.md + USAGE.md already done).

## Task 71: macOS Release build failure + 80 sprintf warnings — DONE (v0.98.30, merged)
GH `Release` macOS leg failed: (1) `'SDL2/SDL.h' file not found` compiling `rewind_buffer.cpp` into
`jnext_debug` (lib linked only spdlog, so no SDL2 include dir; masked on Linux where `/usr/include/SDL2`
is default). Fix: `jnext_debug` links `SDL2::SDL2` (matches `src/input` precedent). (2) 80 `std::sprintf`
deprecation warnings in `src/debug/disasm.cpp` → converted all to `std::snprintf(dst, sizeof(dst), …)`;
`fmt_disp` lambda takes `char(&)[16]` for correct `sizeof`. Triplet 4732/1356/90; independent review
APPROVE (NIT/MINOR only). Cannot verify actual macOS build locally (no Mac) — fixes correct in principle.

## Task 72: `make publish-release` target — DONE (v0.98.31, merged)
Bulk `git push --tags` of >3 tags sends 0 create events → GH Release gate never fires. New target pushes
the branch, then pushes ONLY the newest v* tag (semver `sort -V | tail -1`) alone. Interactive confirm;
warns if tag not in releases.yaml or already on origin. Independent review APPROVE-WITH-NITS; NIT#1
(escape dots in the releases.yaml grep, matching sync-version.sh bb8ea636) applied; NIT#2 (untracked
files not in clean-tree guard) left as-is per bump-* precedent. Static-verified only — the block-push
hook prevents running it here; the target's own [y/N] gate enforces authorization at use.

## Task 73: macOS build STILL failing (objcopy) + manual macOS CI — DONE (v0.98.33, merged)
After Task 71 (SDL2+sprintf), the v0.98.32 macOS Release leg got past Configure and failed at
`src/core/nextboot_rom.o` (exit 127): the ROM-embed step shells out to GNU `objcopy`, absent on macOS
(and the arm64 runner rejects `--binary-architecture=i386:x86-64`). Fix: replaced the objcopy recipe
with `src/core/embed_rom.cmake` — generates a portable C byte array (`nextboot_rom_data`/`_size`);
builds identically on Linux/Windows/macOS, drops the CMAKE_OBJCOPY dependency. Byte-exact (8192).
Also added `.github/workflows/macos-build.yml` — a `workflow_dispatch` (manual, Actions UI) macOS build
with `--version` smoke + optional .dmg, NOT continue-on-error, so the user can iterate without releases.
Triplet 4732/1356/90. Independent review REJECT→APPROVE (stale objcopy refs in packaging/README.md +
CMakeLists.txt comment + CLAUDE.md fixed). Still unverified on real macOS — the manual workflow is the proof.

## Public release v0.98.34 — PREPARED (not pushed; user pushes via `make publish-release`)
macOS build confirmed working by the user (manual macOS-build workflow). Public release prepared per
protocol: ChangeLog v0.98.34 entry FIRST (differential since last public v0.98.29 — macOS support +
build fixes + publish-release tooling), committed; then bump-patch WITH publish (v0.98.34 added to
releases.yaml, metainfo `<release>` synced). Tag v0.98.34 on HEAD. `make bump-patch`'s publish prompt
is TTY-gated (`script` absent), so the publish branch was replicated faithfully (add-release.sh +
version.yaml + sync-version.sh + same 5-file stage set + `chore: bump version to 0.98.34` + tag).
Triplet NOT re-run — v0.98.34 == v0.98.33 (green 4732/1356/90) + ChangeLog/version/packaging metadata,
native-C++-inert. main ahead 2 of origin. User will `make publish-release` (pushes main + single tag
v0.98.34 → gate fires → public GitHub Release incl. now-working macOS leg).

## Task completion status

- [x] Release verification — v0.98.29 GitHub Release published (user verified in GH).
- [~] Task 63 — issue #9 (60 Hz on weak CPU): pacing fixed v0.98.4; OPEN, danboid says needs more investigation.
- [ ] Task 65 — Phase 9 Optimization: refactor/tidy-up, magic-numbers, copper/beast 400% tail.
- [~] Task 67 — Phase 9 Packaging: Linux+Flatpak+Windows done; macOS leg UNVERIFIED (no Mac).
- [ ] Task 68 — Phase 9 Documentation: DEVELOPMENT doc/process + man page.
- [x] Task 71 — macOS Release build fix (SDL2 include) + 80 sprintf→snprintf warnings, MERGED v0.98.30, reviewed APPROVE.
- [x] Task 72 — `make publish-release` target (single-tag push), MERGED v0.98.31, reviewed APPROVE-WITH-NITS (NIT#1 applied).
- [x] Task 73 — macOS objcopy→portable-C-array ROM embed + manual macOS CI workflow, MERGED v0.98.33, reviewed REJECT→APPROVE.
