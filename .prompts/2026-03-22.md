# Tasks

## Task 1

Debug GUI problems:

- When the GUI is loaded with a file (--load) the NEX file loading works fine
- When the GUI is loaded wuthout a file (= run 48K ROM) then:
  - When selecting a NEX file ro tun from the menu, the NEX file does not run correctly. The same NEX file works fine in SDL mode or running it with the GUI witn --load
  - When pressing Reset, the system does not correctly execute the ROM. The reset button should always force a clean boot of the emulator (in 48K mode for the moment)
- The NEX file load function should completely reset the machine and all its peripherals before loading the NEX data (and not be dependent on initial clean configuration from program CRT startup)
- The Reset butto should do the same complete reset

## Task 2

- The CMake targets are named zxnext_... , that's a legacy from before we renamed the emulator to JNext. Rename those modules to jnext_*
- No files named zxnext_* should be generated during the build (if they belong to our source code)
- Files from third party modules are exempt from this requirement

## Task Completion Status

- [x] Task 1: COMPLETE — Fixed 3 bugs:
  1. `PortDispatch::clear_handlers()` added; called in `init()` to prevent handler duplication on reset
  2. `Renderer::reset()` and `Ula::reset()` added; called in `init()` to restore video defaults
  3. `Emulator::load_nex()` now calls `reset()` before loading NEX data, ensuring clean machine state
