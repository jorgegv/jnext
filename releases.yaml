# Tags listed here get a public GitHub Release (packages built + published by
# CI) when the tag is pushed. Everything else is a private history tag that
# builds nothing.
#
# Every `make bump-*` (patch, minor and major) asks whether to add the new tag
# here (default No — a private history tag). Answer yes to make that version a
# public GitHub Release. See doc/RELEASE-PROTOCOL.md.
releases:
  - v0.98.32
  - v0.98.29
  - v0.98.19
  - v0.98.24
